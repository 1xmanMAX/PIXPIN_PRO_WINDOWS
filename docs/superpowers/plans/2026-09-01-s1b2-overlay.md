# PixPin Max · S1-B2 Overlay de selección — Plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Que `Ctrl+Alt+X` congele la pantalla en un overlay por monitor donde el usuario elige la región —con snap a ventanas y controles, lupa 8× con color, tiradores, teclado y modo en vivo— y una barra de resultado que copia, guarda o descarta; y que `Ctrl+Alt+C` haga lo mismo pero directo al portapapeles.

**Architecture:** Toda la interacción del overlay —máquina de estados de ratón y teclado, colocación de la lupa, layout de la barra— es lógica pura y vive en `pixpin-ui` (L3, `forbid(unsafe_code)`), donde se prueba sin abrir una ventana. El dibujo pasa por `pixpin-render` (L1): Direct2D + DirectWrite sobre el **mismo** dispositivo D3D11 de la captura, compuesto por DirectComposition en ventanas `WS_EX_NOREDIRECTIONBITMAP`. Las ventanas de overlay (una por monitor, con su propia clase y su propio `WndProc`) y el hilo de UI Automation viven en `pixpin-shell` (L1). El ejecutable orquesta: captura → overlays → interacción → acción final.

**Tech Stack:** Rust estable edición 2024 · crate `windows` (Direct2D, DirectWrite, DirectComposition, DXGI, UI Automation, Win32) · todo lo ya construido en S1-A y S1-B1.

**Spec:** [`../specs/2026-08-09-s1-cimientos-captura-design.md`](../specs/2026-08-09-s1-cimientos-captura-design.md) §1-§2 (hilos, overlay) — **léela antes de empezar**
**Rendimiento (D13-D19):** [`../specs/2026-08-31-rendimiento-equipos-modestos-design.md`](../specs/2026-08-31-rendimiento-equipos-modestos-design.md) — S1-B2 introduce el **primer consumidor real** de `Nivel`
**Lecciones de S1-A:** [`../retrospectivas/2026-08-09-s1a-lecciones.md`](../retrospectivas/2026-08-09-s1a-lecciones.md) — las notas «para S1-B» aplican literalmente aquí

## Global Constraints

Aplican a **todas** las tareas. No se repiten en cada una.

- **Rust estable, edición 2024. Nada de `nightly`.** Windows 10 21H2 mínimo.
- **`#![forbid(unsafe_code)]`** en `pixpin-geom`, `pixpin-model`, `pixpin-nivel`, `pixpin-store`, `pixpin-ui`, `pixpin-flow`, `pixpin-plugin` y `apps/pixpin`. Si una tarea necesitase `unsafe` en uno de ellos, la frontera se ha roto: **párate y dilo**.
- **`unsafe` permitido y documentado** en `pixpin-shell`, `pixpin-render`, `pixpin-gpu`, `pixpin-codec`, `pixpin-capture`, `pixpin-record`, `pixpin-pin`, `pixpin-ocr`, `pixpin-pdf`. Todo bloque lleva `// SAFETY:` que explica por qué se cumplen las precondiciones; una obligación del llamante se escribe como obligación, nunca como hecho verificado.
- **Regla de capas:** L0 `geom, model, nivel` · L1 `shell, render, gpu, codec` · L2 `capture, pin, pdf, ocr, record, store` · L3 `ui, flow, plugin` · L4 `pixpin`. Sólo hacia abajo; `apps/pixpin/tests/capas.rs` lo hace cumplir. **Nota clave de esta fase:** `pixpin-render` (L1) **no puede** depender de `pixpin-capture` (L2); el dispositivo D3D11 compartido se pasa como `&ID3D11Device` —tipo del crate `windows`, externo, que el test de capas ignora a propósito— y quien cablea ambos es el ejecutable (L4).
- **Baseline del procesador `x86-64` (D17).** Vigilado por `apps/pixpin/tests/baseline.rs`; el SIMD moderno sólo por `is_x86_feature_detected!`.
- **El nivel de rendimiento viaja como dato.** `Nivel` y `Presupuesto` llegan por parámetro desde `apps/pixpin`. Sin globals. En S1-B2 el nivel gobierna: el **tope de FPS del modo en vivo** (`Completo` sin tope; `Ligero` 30 fps) y la **ausencia de animaciones** (que en S1-B2 es trivial: no se implementa ninguna animación, en ningún nivel — se anota para que nadie la añada «de gratis»).
- **Los tres intocables (D14):** atajo→overlay < 50 ms · latencia de interacción ≤ 1 fotograma del refresco real · **mostrar la captura no la muta**: los bytes del PNG guardado tras pasar por el overlay son idénticos a los de guardar sin overlay (Task 12 lo convierte en test).
- **Identificadores en ASCII**, español sin tildes ni eñes. Comentarios y texto de usuario sí llevan tildes. Todo texto visible pasa por Fluent.
- **Los tests corren con `-- --test-threads=1`.** Todo test que exija GPU o sesión de escritorio va `#[ignore]` con su motivo y se ejecuta con `cargo test -- --ignored`.
- **Cero telemetría, cero red.** Comentarios en español. Cada tarea termina en commit.

### Dos reglas de test que S1-A pagó caro

1. **Todo test que proteja una invariante trae su caso negativo explícito.** Cuando un test afirme «X no ocurre», debe existir una entrada para la que **sí** ocurriría con una implementación ingenua.
2. **«Verificado» sólo puede significar «lo ejecuté».** Cada afirmación del informe lleva el comando que la respalda y su salida.

### Tres minas señaladas por la retrospectiva de S1-A — esta fase pisa las tres

- **`VentanaMensajes::procedimiento` llama a `PostQuitMessage` ante cualquier `WM_DESTROY`.** Los overlays NO comparten esa clase ni ese `WndProc`: tienen los suyos (Task 6), y cerrar un overlay no puede matar la aplicación. Hay un test para eso.
- **WGC entrega frames en un hilo del pool.** El modo en vivo usa un canal entre hilos de verdad más `PostMessage` para despertar el bucle (Task 8), nunca la cola `PENDIENTES` del hilo de interfaz.
- **`arranque.rs` maneja un handle sin RAII y sobrevivió de milagro.** Todo handle nuevo de esta fase (ventanas, factorías COM, swapchains) vive en un tipo con `Drop` o en los wrappers del crate `windows`, nunca suelto.

### Estado del que partes

`main` tiene S1-A y S1-B1 fusionados y en verde: ~80 tests más 17 de escritorio/GPU. Disponible y probado:

| Módulo | Qué ofrece |
|---|---|
| `pixpin_geom` | `Punto`, `Rect` (media apertura), `DisposicionMonitores`/`Monitor` (`escala_por_cien`, `area_trabajo`), `Seleccion` (arrastre, 8 `Tirador`, `desplazar`, `sujetar_a`), `Candidato`/`resolver_ajuste` |
| `pixpin_nivel` | `Hechos`, `Nivel::{Completo, Ligero}`, `Preferencia`, `decidir`, `Presupuesto::desde` |
| `pixpin_capture` | `Dispositivo::{nuevo, d3d, contexto, winrt}`, `enumerar_monitores`, `handle_de_monitor`, `capturar_monitor` → `Instantanea::{textura, area, recortar}`, `a_imagen` |
| `pixpin_codec` | `ImagenRgba`, `FormatoImagen::{Png, Jpg, Webp, por_extension}`, `guardar`, `copiar_imagen` |
| `pixpin_shell` | `VentanaMensajes` (sólo-mensajes; **no** sirve para overlays), `atajos`, `Bandeja`, `hechos::recolectar`, `entorno`, `dialogo` |
| `pixpin_store` | `Ajustes` (con `rendimiento.nivel` y `formato_color`), `Catalogo` Fluent, `rutas` |

**El entorno:** `cargo` **no está en el PATH** de las sesiones nuevas: antepón `$env:PATH = "$env:USERPROFILE\.cargo\bin;$env:PATH"` (PowerShell) o `export PATH="$USERPROFILE/.cargo/bin:$PATH"` (bash). El equipo de desarrollo tiene monitor 3000×2000 al 150 %: los binarios de test **no** llevan el manifiesto PerMonitorV2, así que todo test de escritorio que compare píxeles físicos llama primero al helper `asegurar_dpi_fisico()` (patrón establecido en `pixpin-capture/src/instantanea.rs`).

---

## Estructura de ficheros

```
crates/pixpin-ui/              L3 puro, forbid(unsafe_code) -- la interaccion se prueba aqui
  src/lib.rs                   reexporta
  src/overlay.rs               EstadoOverlay: raton, teclado, snap, vivo/congelado (Task 1)
  src/lupa.rs                  region fuente, colocacion, texto de color (Task 2)
  src/barra.rs                 layout y hit-testing de la barra de resultado (Task 3)

crates/pixpin-render/          L1, unsafe permitido -- primer codigo Direct2D del proyecto
  src/lib.rs                   reexporta
  src/motor.rs                 factorias D2D/DWrite + dispositivo D2D sobre un ID3D11Device (Task 4)
  src/superficie.rs            DirectComposition + swapchain por HWND, present a demanda (Task 5)

crates/pixpin-shell/
  src/overlay.rs               VentanaOverlay: clase propia, WndProc propio, eventos (Task 6)
  src/uia.rs                   hilo UI Automation con cache que nunca bloquea (Task 7)
  src/guardar.rs               IFileSaveDialog para "Guardar como" (Task 9)

crates/pixpin-capture/
  src/sesion.rs                SesionViva: WGC continua con tope de FPS por nivel (Task 8)

apps/pixpin/src/overlay.rs     orquestacion: dibujo, entrada, acciones (Task 10)
apps/pixpin/src/main.rs        cableado de Ctrl+Alt+X y Ctrl+Alt+C (Tasks 10-11)
crates/pixpin-store/i18n/*     textos de la barra de resultado (Task 10)
```

**Fuera de alcance de S1-B2:** captura con scroll (`Ctrl+Alt+S`) y cuentagotas global (`Ctrl+Alt+D`) — van en S1-B3, reutilizando la lupa y la sesión viva de aquí. Tampoco hay animaciones ni efectos: el primer shader llega en S2.

---
## Task 1: La máquina de estados del overlay, en puro

Todo lo que el usuario hace con el ratón y el teclado, como función pura de eventos a efectos. Las ventanas de la Task 6 sólo traducen mensajes Win32 a `EventoEntrada` y dibujan lo que el estado diga.

**Files:**
- Create: `crates/pixpin-ui/src/overlay.rs`
- Modify: `crates/pixpin-ui/src/lib.rs`, `crates/pixpin-ui/Cargo.toml`, `apps/pixpin/tests/capas.rs` (nada que cambiar en el conteo: `pixpin-ui` ya existe; sólo comprobar que sigue en verde con la dependencia nueva)

**Interfaces:**
- Consumes: `pixpin_geom::{Punto, Rect, Seleccion, Tirador, Candidato, resolver_ajuste, DisposicionMonitores}`.
- Produces:
  - `pub enum Fase { Explorando, Trazando, Redimensionando, Moviendo, Lista }`
  - `pub enum TeclaOverlay { Flecha { dx: i32, dy: i32 }, Espacio, Escape, Enter }` (el multiplicador ×10 de `Shift` lo aplica quien traduce el mensaje, no esta maquina: aqui llega el delta final)
  - `pub enum EventoEntrada { RatonMovido(Punto), BotonPulsado(Punto), BotonSoltado(Punto), Tecla(TeclaOverlay), Candidatos(Vec<Candidato>) }`
  - `pub enum Efecto { Nada, Redibujar, AlternarVivo, Confirmar(Rect), Cancelar }`
  - `pub enum FormaCursor { Cruz, Mover, RedimNS, RedimEO, RedimNeSo, RedimNoSe }`
  - `pub struct EstadoOverlay { … }` con `pub fn nuevo(disposicion: DisposicionMonitores) -> Self; pub fn procesar(&mut self, e: EventoEntrada) -> Efecto; pub fn fase(&self) -> Fase; pub fn seleccion(&self) -> Rect; pub fn cursor(&self) -> Punto; pub fn vivo(&self) -> bool; pub fn rect_resaltado(&self) -> Option<Rect>; pub fn forma_cursor(&self) -> FormaCursor`

**Reglas de comportamiento** (la spec §2.3; cada una con test):

1. En `Explorando`, mover el ratón resuelve el snap contra los candidatos vigentes: `rect_resaltado()` lo enseña.
2. `BotonPulsado` pasa a `Trazando` con ancla. `BotonSoltado` a menos de 4 px del ancla es un **clic**: adopta el candidato resaltado si lo hay (fase `Lista`); si no lo hay, vuelve a `Explorando`. Soltar tras arrastrar de verdad pasa a `Lista` con lo trazado.
3. En `Lista`: pulsar sobre un tirador (radio 8 px) → `Redimensionando`; dentro de la selección → `Moviendo`; fuera → nuevo `Trazando`.
4. Flechas en `Lista` desplazan y **sujetan al escritorio**; `Enter` confirma (también en `Explorando` con candidato resaltado: es el «clic sin arrastrar» del teclado); `Esc` cancela **siempre**, en cualquier fase; `Espacio` devuelve `AlternarVivo` y conmuta `vivo()`.
5. La selección nunca sale del escritorio virtual: tras cualquier gesto se aplica `sujetar_a`.

- [ ] **Step 1: Añadir la dependencia y escribir los tests que fallan**

```bash
cargo add pixpin-geom --path crates/pixpin-geom -p pixpin-ui
```

Run: `cargo test -p pixpin --test capas -- --test-threads=1`
Expected: PASS — `pixpin-ui` (L3) → `pixpin-geom` (L0) es correcto.

Crear `crates/pixpin-ui/src/overlay.rs` con sólo las pruebas:

```rust
#[cfg(test)]
mod pruebas {
    use super::*;
    use pixpin_geom::{Candidato, DisposicionMonitores, Monitor, Punto, Rect};

    fn escritorio_1080p() -> DisposicionMonitores {
        DisposicionMonitores::nueva(vec![Monitor {
            id: 1,
            area: Rect { x: 0, y: 0, ancho: 1920, alto: 1080 },
            area_trabajo: Rect { x: 0, y: 0, ancho: 1920, alto: 1040 },
            escala_por_cien: 100,
            principal: true,
        }])
    }

    fn estado() -> EstadoOverlay {
        EstadoOverlay::nuevo(escritorio_1080p())
    }

    fn candidato(x: i32, y: i32, ancho: u32, alto: u32) -> Candidato {
        Candidato { rect: Rect { x, y, ancho, alto }, profundidad: 1 }
    }

    #[test]
    fn arrastrar_y_soltar_deja_la_seleccion_lista() {
        let mut e = estado();
        e.procesar(EventoEntrada::BotonPulsado(Punto { x: 100, y: 100 }));
        assert_eq!(e.fase(), Fase::Trazando);
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 300, y: 250 }));
        e.procesar(EventoEntrada::BotonSoltado(Punto { x: 300, y: 250 }));
        assert_eq!(e.fase(), Fase::Lista);
        assert_eq!(e.seleccion(), Rect { x: 100, y: 100, ancho: 200, alto: 150 });
    }

    #[test]
    fn un_clic_sin_arrastre_adopta_el_candidato_resaltado() {
        let mut e = estado();
        e.procesar(EventoEntrada::Candidatos(vec![candidato(50, 50, 400, 300)]));
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 200, y: 200 }));
        assert_eq!(e.rect_resaltado(), Some(Rect { x: 50, y: 50, ancho: 400, alto: 300 }));
        e.procesar(EventoEntrada::BotonPulsado(Punto { x: 200, y: 200 }));
        // Se movio 2 px: sigue contando como clic, no como arrastre.
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 202, y: 201 }));
        e.procesar(EventoEntrada::BotonSoltado(Punto { x: 202, y: 201 }));
        assert_eq!(e.fase(), Fase::Lista);
        assert_eq!(e.seleccion(), Rect { x: 50, y: 50, ancho: 400, alto: 300 });
    }

    #[test]
    fn un_clic_sin_candidato_no_deja_seleccion() {
        // Caso negativo del anterior: sin candidatos, el clic suelto no puede
        // inventarse un rectangulo.
        let mut e = estado();
        e.procesar(EventoEntrada::BotonPulsado(Punto { x: 200, y: 200 }));
        e.procesar(EventoEntrada::BotonSoltado(Punto { x: 201, y: 200 }));
        assert_eq!(e.fase(), Fase::Explorando);
    }

    #[test]
    fn en_lista_el_tirador_redimensiona_y_el_interior_mueve() {
        let mut e = estado();
        e.procesar(EventoEntrada::BotonPulsado(Punto { x: 100, y: 100 }));
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 300, y: 200 }));
        e.procesar(EventoEntrada::BotonSoltado(Punto { x: 300, y: 200 }));

        // Esquina sureste: redimensiona.
        e.procesar(EventoEntrada::BotonPulsado(Punto { x: 300, y: 200 }));
        assert_eq!(e.fase(), Fase::Redimensionando);
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 400, y: 300 }));
        e.procesar(EventoEntrada::BotonSoltado(Punto { x: 400, y: 300 }));
        assert_eq!(e.seleccion(), Rect { x: 100, y: 100, ancho: 300, alto: 200 });

        // Interior: mueve sin cambiar el tamano.
        e.procesar(EventoEntrada::BotonPulsado(Punto { x: 200, y: 200 }));
        assert_eq!(e.fase(), Fase::Moviendo);
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 250, y: 220 }));
        e.procesar(EventoEntrada::BotonSoltado(Punto { x: 250, y: 220 }));
        assert_eq!(e.seleccion(), Rect { x: 150, y: 120, ancho: 300, alto: 200 });
    }

    #[test]
    fn pulsar_fuera_de_la_seleccion_empieza_una_nueva() {
        let mut e = estado();
        e.procesar(EventoEntrada::BotonPulsado(Punto { x: 100, y: 100 }));
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 200, y: 200 }));
        e.procesar(EventoEntrada::BotonSoltado(Punto { x: 200, y: 200 }));
        e.procesar(EventoEntrada::BotonPulsado(Punto { x: 800, y: 800 }));
        assert_eq!(e.fase(), Fase::Trazando);
    }

    #[test]
    fn las_flechas_desplazan_y_la_sujecion_impide_salir() {
        let mut e = estado();
        e.procesar(EventoEntrada::BotonPulsado(Punto { x: 10, y: 10 }));
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 110, y: 110 }));
        e.procesar(EventoEntrada::BotonSoltado(Punto { x: 110, y: 110 }));

        e.procesar(EventoEntrada::Tecla(TeclaOverlay::Flecha { dx: -1, dy: 0 }));
        assert_eq!(e.seleccion().x, 9);
        // Caso negativo de la sujecion: veinte pasos de -10 no pueden dejar
        // la seleccion en x negativa; se recorta contra el escritorio.
        for _ in 0..20 {
            e.procesar(EventoEntrada::Tecla(TeclaOverlay::Flecha { dx: -10, dy: 0 }));
        }
        assert_eq!(e.seleccion().x, 0);
        assert!(!e.seleccion().esta_vacio(), "la sujecion no debe vaciar la seleccion");
    }

    #[test]
    fn enter_confirma_escape_cancela_espacio_alterna() {
        let mut e = estado();
        e.procesar(EventoEntrada::BotonPulsado(Punto { x: 100, y: 100 }));
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 300, y: 300 }));
        e.procesar(EventoEntrada::BotonSoltado(Punto { x: 300, y: 300 }));

        assert!(!e.vivo());
        assert_eq!(e.procesar(EventoEntrada::Tecla(TeclaOverlay::Espacio)), Efecto::AlternarVivo);
        assert!(e.vivo());

        assert_eq!(
            e.procesar(EventoEntrada::Tecla(TeclaOverlay::Enter)),
            Efecto::Confirmar(Rect { x: 100, y: 100, ancho: 200, alto: 200 })
        );

        // Escape cancela desde cualquier fase, incluida Trazando a medias.
        let mut e2 = estado();
        e2.procesar(EventoEntrada::BotonPulsado(Punto { x: 5, y: 5 }));
        assert_eq!(e2.procesar(EventoEntrada::Tecla(TeclaOverlay::Escape)), Efecto::Cancelar);
    }

    #[test]
    fn enter_en_explorando_confirma_el_candidato_resaltado() {
        let mut e = estado();
        e.procesar(EventoEntrada::Candidatos(vec![candidato(50, 50, 400, 300)]));
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 100, y: 100 }));
        assert_eq!(
            e.procesar(EventoEntrada::Tecla(TeclaOverlay::Enter)),
            Efecto::Confirmar(Rect { x: 50, y: 50, ancho: 400, alto: 300 })
        );
        // Caso negativo: sin candidato bajo el cursor, Enter no confirma nada.
        let mut e2 = estado();
        assert_eq!(e2.procesar(EventoEntrada::Tecla(TeclaOverlay::Enter)), Efecto::Nada);
    }

    #[test]
    fn la_forma_del_cursor_refleja_lo_que_haria_el_clic() {
        let mut e = estado();
        e.procesar(EventoEntrada::BotonPulsado(Punto { x: 100, y: 100 }));
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 300, y: 200 }));
        e.procesar(EventoEntrada::BotonSoltado(Punto { x: 300, y: 200 }));

        e.procesar(EventoEntrada::RatonMovido(Punto { x: 200, y: 150 }));
        assert_eq!(e.forma_cursor(), FormaCursor::Mover);
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 300, y: 200 }));
        assert_eq!(e.forma_cursor(), FormaCursor::RedimNoSe); // esquina SE
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 300, y: 150 }));
        assert_eq!(e.forma_cursor(), FormaCursor::RedimEO); // borde este
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 900, y: 900 }));
        assert_eq!(e.forma_cursor(), FormaCursor::Cruz);
    }

    #[test]
    fn los_candidatos_nuevos_no_interrumpen_un_gesto_en_curso() {
        // El hilo UIA contesta cuando quiere. Si su respuesta llegara en mitad
        // de un arrastre y cambiara la seleccion, el rectangulo saltaria bajo
        // la mano del usuario.
        let mut e = estado();
        e.procesar(EventoEntrada::BotonPulsado(Punto { x: 100, y: 100 }));
        e.procesar(EventoEntrada::RatonMovido(Punto { x: 200, y: 200 }));
        e.procesar(EventoEntrada::Candidatos(vec![candidato(0, 0, 1900, 1000)]));
        e.procesar(EventoEntrada::BotonSoltado(Punto { x: 200, y: 200 }));
        assert_eq!(e.seleccion(), Rect { x: 100, y: 100, ancho: 100, alto: 100 });
    }
}
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `cargo test -p pixpin-ui overlay -- --test-threads=1`
Expected: FAIL — no existe `EstadoOverlay`.

- [ ] **Step 3: Implementar**

Añadir al principio de `crates/pixpin-ui/src/overlay.rs`:

```rust
//! La maquina de estados del overlay de seleccion.
//!
//! Aqui vive TODO lo que el usuario hace con el raton y el teclado. Las
//! ventanas reales (pixpin_shell::overlay) solo traducen mensajes Win32 a
//! `EventoEntrada` y dibujan lo que este estado diga, de modo que la
//! interaccion completa se prueba sin abrir una ventana.
//!
//! Los candidatos de snap llegan por evento (`EventoEntrada::Candidatos`)
//! porque el hilo de UIA contesta cuando quiere: la regla de la spec es que
//! el overlay NUNCA espera a nadie.

use pixpin_geom::{Candidato, DisposicionMonitores, Punto, Rect, Seleccion, Tirador, resolver_ajuste};

/// Distancia maxima, en pixeles fisicos, para que pulsar+soltar cuente como
/// clic y no como arrastre.
const UMBRAL_CLIC: i32 = 4;
/// Radio de los tiradores, igual que en `Seleccion::tirador_en`.
const RADIO_TIRADOR: u32 = 8;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Fase {
    Explorando,
    Trazando,
    Redimensionando,
    Moviendo,
    Lista,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum TeclaOverlay {
    Flecha { dx: i32, dy: i32 },
    Espacio,
    Escape,
    Enter,
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum EventoEntrada {
    RatonMovido(Punto),
    BotonPulsado(Punto),
    BotonSoltado(Punto),
    Tecla(TeclaOverlay),
    Candidatos(Vec<Candidato>),
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Efecto {
    Nada,
    Redibujar,
    AlternarVivo,
    Confirmar(Rect),
    Cancelar,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum FormaCursor {
    Cruz,
    Mover,
    RedimNS,
    RedimEO,
    /// Noreste-suroeste (la diagonal /).
    RedimNeSo,
    /// Noroeste-sureste (la diagonal \).
    RedimNoSe,
}

#[derive(Debug)]
pub struct EstadoOverlay {
    disposicion: DisposicionMonitores,
    seleccion: Seleccion,
    fase: Fase,
    vivo: bool,
    cursor: Punto,
    ancla_clic: Punto,
    ultimo_cursor: Punto,
    candidatos: Vec<Candidato>,
}

impl EstadoOverlay {
    pub fn nuevo(disposicion: DisposicionMonitores) -> Self {
        Self {
            disposicion,
            seleccion: Seleccion::nueva(),
            fase: Fase::Explorando,
            vivo: false,
            cursor: Punto { x: 0, y: 0 },
            ancla_clic: Punto { x: 0, y: 0 },
            ultimo_cursor: Punto { x: 0, y: 0 },
            candidatos: Vec::new(),
        }
    }

    pub fn fase(&self) -> Fase {
        self.fase
    }

    pub fn seleccion(&self) -> Rect {
        self.seleccion.rect()
    }

    pub fn cursor(&self) -> Punto {
        self.cursor
    }

    pub fn vivo(&self) -> bool {
        self.vivo
    }

    /// El rectangulo del snap bajo el cursor, solo mientras se explora.
    pub fn rect_resaltado(&self) -> Option<Rect> {
        if self.fase != Fase::Explorando {
            return None;
        }
        resolver_ajuste(&self.candidatos, self.cursor)
    }

    pub fn forma_cursor(&self) -> FormaCursor {
        if self.fase == Fase::Lista {
            if let Some(t) = self.seleccion.tirador_en(self.cursor, RADIO_TIRADOR) {
                return match t {
                    Tirador::NorteBorde | Tirador::SurBorde => FormaCursor::RedimNS,
                    Tirador::EsteBorde | Tirador::OesteBorde => FormaCursor::RedimEO,
                    Tirador::NoresteEsquina | Tirador::SuroesteEsquina => FormaCursor::RedimNeSo,
                    Tirador::NoroesteEsquina | Tirador::SuresteEsquina => FormaCursor::RedimNoSe,
                };
            }
            if self.seleccion.rect().contiene(self.cursor) {
                return FormaCursor::Mover;
            }
        }
        FormaCursor::Cruz
    }

    pub fn procesar(&mut self, evento: EventoEntrada) -> Efecto {
        match evento {
            EventoEntrada::Candidatos(c) => {
                // Nunca interrumpen un gesto: solo alimentan el resaltado.
                self.candidatos = c;
                if self.fase == Fase::Explorando { Efecto::Redibujar } else { Efecto::Nada }
            }
            EventoEntrada::RatonMovido(p) => self.raton_movido(p),
            EventoEntrada::BotonPulsado(p) => self.boton_pulsado(p),
            EventoEntrada::BotonSoltado(p) => self.boton_soltado(p),
            EventoEntrada::Tecla(t) => self.tecla(t),
        }
    }

    fn raton_movido(&mut self, p: Punto) -> Efecto {
        self.cursor = p;
        match self.fase {
            Fase::Trazando | Fase::Redimensionando => {
                self.seleccion.arrastrar_a(p);
                Efecto::Redibujar
            }
            Fase::Moviendo => {
                self.seleccion
                    .desplazar(p.x - self.ultimo_cursor.x, p.y - self.ultimo_cursor.y);
                self.deslizar_dentro(); // desliza SIN encoger; ver nota abajo
                self.ultimo_cursor = p;
                Efecto::Redibujar
            }
            _ => Efecto::Redibujar,
        }
    }

    fn boton_pulsado(&mut self, p: Punto) -> Efecto {
        self.cursor = p;
        self.ancla_clic = p;
        self.ultimo_cursor = p;
        match self.fase {
            Fase::Lista => {
                if let Some(t) = self.seleccion.tirador_en(p, RADIO_TIRADOR) {
                    self.seleccion.iniciar_redimension(t);
                    self.fase = Fase::Redimensionando;
                } else if self.seleccion.rect().contiene(p) {
                    self.fase = Fase::Moviendo;
                } else {
                    self.seleccion.iniciar_arrastre(p);
                    self.fase = Fase::Trazando;
                }
            }
            _ => {
                self.seleccion.iniciar_arrastre(p);
                self.fase = Fase::Trazando;
            }
        }
        Efecto::Redibujar
    }

    fn boton_soltado(&mut self, p: Punto) -> Efecto {
        self.cursor = p;
        let fue_clic = (p.x - self.ancla_clic.x).abs() <= UMBRAL_CLIC
            && (p.y - self.ancla_clic.y).abs() <= UMBRAL_CLIC;
        match self.fase {
            Fase::Trazando if fue_clic => {
                self.seleccion.terminar_arrastre();
                // El clic adopta el candidato resaltado; sin el no hay nada.
                match resolver_ajuste(&self.candidatos, p) {
                    Some(r) => {
                        self.seleccion.establecer(r);
                        self.seleccion.sujetar_a(&self.disposicion);
                        self.fase = Fase::Lista;
                    }
                    None => self.fase = Fase::Explorando,
                }
            }
            Fase::Trazando | Fase::Redimensionando | Fase::Moviendo => {
                self.seleccion.terminar_arrastre();
                self.seleccion.sujetar_a(&self.disposicion);
                self.fase = Fase::Lista;
            }
            _ => {}
        }
        Efecto::Redibujar
    }

    fn tecla(&mut self, t: TeclaOverlay) -> Efecto {
        match t {
            TeclaOverlay::Escape => Efecto::Cancelar,
            TeclaOverlay::Espacio => {
                self.vivo = !self.vivo;
                Efecto::AlternarVivo
            }
            TeclaOverlay::Enter => match self.fase {
                Fase::Lista => Efecto::Confirmar(self.seleccion.rect()),
                Fase::Explorando => match self.rect_resaltado() {
                    Some(r) => Efecto::Confirmar(r),
                    None => Efecto::Nada,
                },
                _ => Efecto::Nada,
            },
            TeclaOverlay::Flecha { dx, dy } => {
                if self.fase == Fase::Lista {
                    self.seleccion.desplazar(dx, dy);
                    self.deslizar_dentro();
                    Efecto::Redibujar
                } else {
                    Efecto::Nada
                }
            }
        }
    }
}
```

**Corrección encontrada al ejecutar (ronda 1):** el borrador usaba `sujetar_a` (que RECORTA) para
las flechas y para `Moviendo`, y el caso negativo del test lo cazó: empujar contra el borde iba
encogiendo la selección hasta vaciarla. Un desplazamiento se sujeta con `deslizar_dentro()` —
método privado de `EstadoOverlay` que hace `clamp` de `x`/`y` contra el escritorio virtual **sin
cambiar el tamaño**. `sujetar_a` queda solo para cerrar gestos que sobresalen (trazado, redimensión
y el clic que adopta candidato).

Reemplazar `crates/pixpin-ui/src/lib.rs`:

```rust
//! pixpin-ui — la interaccion como logica pura.
//!
//! Nada de este crate llama a Win32. Dibuja a traves de `pixpin-render` y
//! recibe eventos ya traducidos, de modo que el comportamiento del overlay
//! completo se prueba en milisegundos y sin escritorio. Si algun dia este
//! crate necesitase Win32, la frontera se ha roto: arreglar el diseno, no
//! relajar la regla.
#![forbid(unsafe_code)]

pub mod overlay;

pub use overlay::{Efecto, EstadoOverlay, EventoEntrada, Fase, FormaCursor, TeclaOverlay};
```

- [ ] **Step 4: Ejecutar y comprobar que pasa**

Run: `cargo test -p pixpin-ui -- --test-threads=1`
Expected: PASS — los diez tests.

- [ ] **Step 5: Comprobar el workspace y confirmar**

Run: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace -- --test-threads=1`

```bash
git add crates/pixpin-ui Cargo.lock
git commit -m "Maquina de estados del overlay, pura y probada sin ventanas

Todo lo que el usuario hace con el raton y el teclado vive en pixpin-ui;
las ventanas reales solo traducen mensajes y dibujan. Dos decisiones que
merecen sus tests: los candidatos del hilo UIA nunca interrumpen un gesto
en curso (el rectangulo no salta bajo la mano), y un clic sin arrastre
adopta el candidato resaltado, que es el gesto de capturar una ventana
entera sin trazarla."
```

---

## Task 2: La lupa, en puro

Dónde se coloca, qué región amplía y cómo escribe el color. El dibujo real lo hace la Task 10; aquí sólo la aritmética, que es donde están los errores.

**Files:**
- Create: `crates/pixpin-ui/src/lupa.rs`
- Modify: `crates/pixpin-ui/src/lib.rs`

**Interfaces:**
- Consumes: `pixpin_geom::{Punto, Rect}`, `pixpin_store` **no** (el formato de color llega como parámetro propio para no subir de capa).
- Produces:
  - `pub struct Lupa { pub factor: u32, pub diametro: u32 }` con `pub fn por_defecto(escala_por_cien: u32) -> Lupa` (factor 8; diámetro 176 px lógicos escalados al DPI del monitor)
  - `impl Lupa { pub fn region_fuente(&self, cursor: Punto, monitor: Rect) -> Rect; pub fn colocar(&self, cursor: Punto, monitor: Rect) -> Punto }`
  - `pub enum FormatoColorLupa { Hex, Rgb, Hsl }`
  - `pub fn texto_color(formato: FormatoColorLupa, rgba: [u8; 4]) -> String`

- [ ] **Step 1: Escribir los tests que fallan**

Crear `crates/pixpin-ui/src/lupa.rs` con sólo las pruebas:

```rust
#[cfg(test)]
mod pruebas {
    use super::*;
    use pixpin_geom::{Punto, Rect};

    fn monitor() -> Rect {
        Rect { x: 0, y: 0, ancho: 1920, alto: 1080 }
    }

    #[test]
    fn el_diametro_escala_con_el_dpi_del_monitor() {
        assert_eq!(Lupa::por_defecto(100).diametro, 176);
        assert_eq!(Lupa::por_defecto(150).diametro, 264);
        assert_eq!(Lupa::por_defecto(200).diametro, 352);
        assert_eq!(Lupa::por_defecto(100).factor, 8);
    }

    #[test]
    fn la_region_fuente_esta_centrada_y_mide_diametro_entre_factor() {
        let lupa = Lupa { factor: 8, diametro: 176 };
        let r = lupa.region_fuente(Punto { x: 500, y: 400 }, monitor());
        assert_eq!(r.ancho, 22); // 176 / 8
        assert_eq!(r.alto, 22);
        // Centrada: el cursor cae en el pixel central.
        assert_eq!(r.x, 500 - 11);
        assert_eq!(r.y, 400 - 11);
    }

    #[test]
    fn la_region_fuente_no_se_sale_del_monitor_en_las_esquinas() {
        // Caso negativo: sin el ajuste, en la esquina la region tendria
        // coordenadas negativas y el recorte de textura fallaria.
        let lupa = Lupa { factor: 8, diametro: 176 };
        let r = lupa.region_fuente(Punto { x: 2, y: 2 }, monitor());
        assert_eq!((r.x, r.y), (0, 0));
        assert_eq!((r.ancho, r.alto), (22, 22));
        let r2 = lupa.region_fuente(Punto { x: 1919, y: 1079 }, monitor());
        assert_eq!(r2.derecha(), 1920);
        assert_eq!(r2.abajo(), 1080);
    }

    #[test]
    fn la_lupa_huye_del_cursor_al_acercarse_al_borde() {
        let lupa = Lupa { factor: 8, diametro: 176 };
        // Lejos de los bordes: abajo a la derecha del cursor.
        let p = lupa.colocar(Punto { x: 500, y: 400 }, monitor());
        assert!(p.x > 500 && p.y > 400);
        // Cerca del borde derecho: se va a la izquierda.
        let p = lupa.colocar(Punto { x: 1900, y: 400 }, monitor());
        assert!(p.x + 176 <= 1920, "se saldria por la derecha");
        assert!(p.x < 1900);
        // Esquina inferior derecha: arriba a la izquierda, y entera dentro.
        let p = lupa.colocar(Punto { x: 1910, y: 1070 }, monitor());
        assert!(p.x + 176 <= 1920 && p.y + 176 <= 1080);
    }

    #[test]
    fn el_texto_de_color_sale_en_los_tres_formatos() {
        assert_eq!(texto_color(FormatoColorLupa::Hex, [255, 87, 51, 255]), "#FF5733");
        assert_eq!(texto_color(FormatoColorLupa::Rgb, [255, 87, 51, 255]), "rgb(255, 87, 51)");
        // HSL de rojo puro: matiz 0, saturacion 100%, luminosidad 50%.
        assert_eq!(texto_color(FormatoColorLupa::Hsl, [255, 0, 0, 255]), "hsl(0, 100%, 50%)");
        // Gris puro: saturacion 0; una division por cero ingenua daria NaN.
        assert_eq!(texto_color(FormatoColorLupa::Hsl, [128, 128, 128, 255]), "hsl(0, 0%, 50%)");
        // Verde puro: matiz 120.
        assert_eq!(texto_color(FormatoColorLupa::Hsl, [0, 255, 0, 255]), "hsl(120, 100%, 50%)");
    }
}
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `cargo test -p pixpin-ui lupa -- --test-threads=1`
Expected: FAIL — no existe `Lupa`.

- [ ] **Step 3: Implementar**

Añadir al principio de `crates/pixpin-ui/src/lupa.rs`:

```rust
//! La aritmetica de la lupa: que region amplia, donde se coloca y como
//! escribe el color. Esta pieza es tambien el cuentagotas del catalogo
//! (S1-B3 la reutiliza tal cual): mismo codigo, dos funciones cubiertas.

use pixpin_geom::{Punto, Rect};

/// Separacion entre el cursor y la esquina de la lupa, en pixeles fisicos.
const MARGEN_CURSOR: i32 = 24;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct Lupa {
    /// Aumento: 8 pixeles dibujados por cada pixel real.
    pub factor: u32,
    /// Lado del cuadrado de la lupa, en pixeles fisicos del monitor.
    pub diametro: u32,
}

impl Lupa {
    /// Lupa de la spec: 8x, 176 px logicos escalados al DPI del monitor.
    pub fn por_defecto(escala_por_cien: u32) -> Lupa {
        Lupa { factor: 8, diametro: 176 * escala_por_cien / 100 }
    }

    /// Region del monitor que la lupa amplia, centrada en el cursor y
    /// desplazada (no encogida) para no salirse del monitor: encogerla
    /// cambiaria el aumento en los bordes.
    pub fn region_fuente(&self, cursor: Punto, monitor: Rect) -> Rect {
        let lado = (self.diametro / self.factor) as i32;
        let mut x = cursor.x - lado / 2;
        let mut y = cursor.y - lado / 2;
        x = x.clamp(monitor.izquierda(), monitor.derecha() - lado);
        y = y.clamp(monitor.arriba(), monitor.abajo() - lado);
        Rect { x, y, ancho: lado as u32, alto: lado as u32 }
    }

    /// Esquina superior izquierda donde dibujar la lupa: en el cuadrante
    /// opuesto al borde mas cercano, para no tapar lo que se esta mirando.
    pub fn colocar(&self, cursor: Punto, monitor: Rect) -> Punto {
        let d = self.diametro as i32;
        let mut x = cursor.x + MARGEN_CURSOR;
        let mut y = cursor.y + MARGEN_CURSOR;
        if x + d > monitor.derecha() {
            x = cursor.x - MARGEN_CURSOR - d;
        }
        if y + d > monitor.abajo() {
            y = cursor.y - MARGEN_CURSOR - d;
        }
        Punto {
            x: x.clamp(monitor.izquierda(), monitor.derecha() - d),
            y: y.clamp(monitor.arriba(), monitor.abajo() - d),
        }
    }
}

/// Copia local del formato de `pixpin-store` para no depender hacia arriba:
/// `pixpin-ui` es L3 y `pixpin-store` L2, pero la conversion la decide quien
/// cablea (el ejecutable), no este crate.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum FormatoColorLupa {
    Hex,
    Rgb,
    Hsl,
}

/// El color bajo el cursor, como texto listo para ensenar y copiar.
pub fn texto_color(formato: FormatoColorLupa, rgba: [u8; 4]) -> String {
    let [r, g, b, _] = rgba;
    match formato {
        FormatoColorLupa::Hex => format!("#{r:02X}{g:02X}{b:02X}"),
        FormatoColorLupa::Rgb => format!("rgb({r}, {g}, {b})"),
        FormatoColorLupa::Hsl => {
            // Conversion clasica en aritmetica entera de milesimas, para que
            // el resultado sea determinista y sin sorpresas de coma flotante.
            let (r, g, b) = (r as i32, g as i32, b as i32);
            let max = r.max(g).max(b);
            let min = r.min(g).min(b);
            let l_mil = (max + min) * 1000 / 510; // luminosidad en milesimas
            let delta = max - min;
            let (h, s_mil) = if delta == 0 {
                (0, 0)
            } else {
                let s_mil = if l_mil > 500 {
                    delta * 1000 / (510 - max - min)
                } else {
                    delta * 1000 / (max + min)
                };
                let h = if max == r {
                    (60 * (g - b) / delta).rem_euclid(360)
                } else if max == g {
                    60 * (b - r) / delta + 120
                } else {
                    60 * (r - g) / delta + 240
                };
                (h, s_mil)
            };
            format!(
                "hsl({h}, {}%, {}%)",
                (s_mil + 5) / 10,
                (l_mil + 5) / 10
            )
        }
    }
}
```

Añadir a `crates/pixpin-ui/src/lib.rs`:

```rust
pub mod lupa;

pub use lupa::{FormatoColorLupa, Lupa, texto_color};
```

- [ ] **Step 4: Ejecutar y comprobar que pasa**

Run: `cargo test -p pixpin-ui -- --test-threads=1`
Expected: PASS. Si el HSL da 99% o 51% por redondeo, ajusta el redondeo entero (`+5)/10`) hasta que los tres casos canonicos salgan exactos — son valores de tabla, no aproximaciones.

- [ ] **Step 5: Comprobar el workspace y confirmar**

Run: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace -- --test-threads=1`

```bash
git add crates/pixpin-ui
git commit -m "La lupa como aritmetica pura: region fuente, colocacion y color

La region fuente se desplaza en los bordes en vez de encogerse, porque
encogerla cambiaria el aumento justo donde mas se mira. La lupa huye del
cursor cuadrante a cuadrante para no tapar lo que se esta midiendo. El HSL
va en aritmetica entera de milesimas: determinista, y con el caso del gris
puro cubierto, donde una division ingenua por cero daria NaN.

Esta pieza es tambien el cuentagotas del catalogo: S1-B3 la reutiliza."
```

---

## Task 3: La barra de resultado, en puro

Layout y hit-testing de los cuatro botones. El dibujo y los textos reales llegan en la Task 10.

**Files:**
- Create: `crates/pixpin-ui/src/barra.rs`
- Modify: `crates/pixpin-ui/src/lib.rs`

**Interfaces:**
- Consumes: `pixpin_geom::{Punto, Rect}`.
- Produces:
  - `pub enum AccionBarra { Copiar, Guardar, GuardarComo, Descartar }`
  - `pub struct Barra { pub origen: Rect }` con `pub fn colocar(seleccion: Rect, area_trabajo: Rect, escala_por_cien: u32) -> Barra; pub fn boton_en(&self, p: Punto) -> Option<AccionBarra>; pub fn rect_boton(&self, a: AccionBarra) -> Rect; pub const ACCIONES: [AccionBarra; 4]`

- [ ] **Step 1: Escribir los tests que fallan**

Crear `crates/pixpin-ui/src/barra.rs` con sólo las pruebas:

```rust
#[cfg(test)]
mod pruebas {
    use super::*;
    use pixpin_geom::{Punto, Rect};

    fn area_trabajo() -> Rect {
        Rect { x: 0, y: 0, ancho: 1920, alto: 1040 }
    }

    #[test]
    fn la_barra_se_coloca_debajo_de_la_seleccion() {
        let sel = Rect { x: 400, y: 300, ancho: 500, alto: 200 };
        let b = Barra::colocar(sel, area_trabajo(), 100);
        assert!(b.origen.arriba() > sel.abajo());
        // Alineada al borde derecho de la seleccion, como las herramientas
        // de captura que la gente ya conoce.
        assert_eq!(b.origen.derecha(), sel.derecha());
    }

    #[test]
    fn sin_sitio_debajo_la_barra_sube_encima() {
        let sel = Rect { x: 400, y: 800, ancho: 500, alto: 220 };
        let b = Barra::colocar(sel, area_trabajo(), 100);
        assert!(b.origen.abajo() < sel.arriba() + 1, "deberia quedar encima");
        assert!(area_trabajo().interseccion(b.origen) == Some(b.origen), "y dentro del area de trabajo");
    }

    #[test]
    fn con_la_seleccion_a_pantalla_completa_la_barra_queda_dentro() {
        // Caso negativo del layout: ni debajo ni encima hay sitio. Si la
        // implementacion solo probara esas dos, la barra quedaria fuera de
        // pantalla e inalcanzable.
        let sel = Rect { x: 0, y: 0, ancho: 1920, alto: 1040 };
        let b = Barra::colocar(sel, area_trabajo(), 100);
        assert_eq!(area_trabajo().interseccion(b.origen), Some(b.origen));
    }

    #[test]
    fn la_barra_escala_con_el_dpi() {
        let sel = Rect { x: 100, y: 100, ancho: 300, alto: 200 };
        let cien = Barra::colocar(sel, area_trabajo(), 100);
        let doscientos = Barra::colocar(sel, area_trabajo(), 200);
        assert_eq!(doscientos.origen.alto, cien.origen.alto * 2);
        assert_eq!(doscientos.origen.ancho, cien.origen.ancho * 2);
    }

    #[test]
    fn cada_boton_responde_en_su_rectangulo_y_fuera_nadie() {
        let sel = Rect { x: 400, y: 300, ancho: 500, alto: 200 };
        let b = Barra::colocar(sel, area_trabajo(), 100);
        for accion in Barra::ACCIONES {
            let r = b.rect_boton(accion);
            let centro = Punto { x: r.x + (r.ancho / 2) as i32, y: r.y + (r.alto / 2) as i32 };
            assert_eq!(b.boton_en(centro), Some(accion), "fallo en {accion:?}");
        }
        // Caso negativo: un punto fuera de la barra no responde.
        assert_eq!(b.boton_en(Punto { x: 0, y: 0 }), None);
        // Y el hueco entre dos botones tampoco.
        let primero = b.rect_boton(Barra::ACCIONES[0]);
        assert_eq!(
            b.boton_en(Punto { x: primero.derecha() + 1, y: primero.y + 4 }),
            None,
            "el hueco entre botones no debe activar ninguno"
        );
    }
}
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `cargo test -p pixpin-ui barra -- --test-threads=1`
Expected: FAIL — no existe `Barra`.

- [ ] **Step 3: Implementar**

Añadir al principio de `crates/pixpin-ui/src/barra.rs`:

```rust
//! La barra de resultado: cuatro botones junto a la seleccion confirmada.
//!
//! En S2 esta barra crece hasta ser la entrada al editor y en S3 gana el
//! boton de pinear (la spec lo dice explicitamente), asi que el layout es
//! una lista de acciones, no cuatro rectangulos con nombre.

use pixpin_geom::{Punto, Rect};

/// Medidas base en pixeles logicos (al 100% de escala).
const ALTO_LOGICO: u32 = 40;
const ANCHO_BOTON_LOGICO: u32 = 96;
const HUECO_LOGICO: u32 = 4;
const SEPARACION_LOGICA: u32 = 8;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum AccionBarra {
    Copiar,
    Guardar,
    GuardarComo,
    Descartar,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct Barra {
    pub origen: Rect,
    escala_por_cien: u32,
}

impl Barra {
    pub const ACCIONES: [AccionBarra; 4] = [
        AccionBarra::Copiar,
        AccionBarra::Guardar,
        AccionBarra::GuardarComo,
        AccionBarra::Descartar,
    ];

    /// Debajo de la seleccion; si no cabe, encima; si tampoco, dentro,
    /// pegada al borde inferior. Siempre integra en el area de trabajo.
    pub fn colocar(seleccion: Rect, area_trabajo: Rect, escala_por_cien: u32) -> Barra {
        let e = |v: u32| v * escala_por_cien / 100;
        let alto = e(ALTO_LOGICO);
        let ancho = e(ANCHO_BOTON_LOGICO) * Self::ACCIONES.len() as u32
            + e(HUECO_LOGICO) * (Self::ACCIONES.len() as u32 - 1);
        let sep = e(SEPARACION_LOGICA) as i32;

        // Alineada al borde derecho de la seleccion, sin salirse por la
        // izquierda del area de trabajo.
        let x = (seleccion.derecha() - ancho as i32)
            .clamp(area_trabajo.izquierda(), area_trabajo.derecha() - ancho as i32);

        let debajo = seleccion.abajo() + sep;
        let encima = seleccion.arriba() - sep - alto as i32;
        let y = if debajo + (alto as i32) <= area_trabajo.abajo() {
            debajo
        } else if encima >= area_trabajo.arriba() {
            encima
        } else {
            // Dentro de la seleccion, pegada abajo.
            seleccion.abajo() - sep - alto as i32
        };

        Barra { origen: Rect { x, y, ancho, alto }, escala_por_cien }
    }

    pub fn rect_boton(&self, accion: AccionBarra) -> Rect {
        let e = |v: u32| v * self.escala_por_cien / 100;
        let indice = Self::ACCIONES.iter().position(|a| *a == accion).unwrap() as u32;
        Rect {
            x: self.origen.x + (indice * (e(ANCHO_BOTON_LOGICO) + e(HUECO_LOGICO))) as i32,
            y: self.origen.y,
            ancho: e(ANCHO_BOTON_LOGICO),
            alto: self.origen.alto,
        }
    }

    pub fn boton_en(&self, p: Punto) -> Option<AccionBarra> {
        Self::ACCIONES.into_iter().find(|a| self.rect_boton(*a).contiene(p))
    }
}
```

Añadir a `crates/pixpin-ui/src/lib.rs`:

```rust
pub mod barra;

pub use barra::{AccionBarra, Barra};
```

- [ ] **Step 4: Ejecutar y comprobar que pasa**

Run: `cargo test -p pixpin-ui -- --test-threads=1`
Expected: PASS — los cinco de barra más los anteriores.

- [ ] **Step 5: Comprobar el workspace y confirmar**

Run: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace -- --test-threads=1`

```bash
git add crates/pixpin-ui
git commit -m "Barra de resultado: layout y hit-testing puros

Debajo de la seleccion; si no cabe, encima; si tampoco, dentro pegada
abajo — el caso negativo es la seleccion a pantalla completa, donde una
implementacion que solo probara debajo y encima dejaria la barra fuera de
pantalla e inalcanzable. Es una lista de acciones y no cuatro rectangulos
con nombre porque en S2 crece hasta ser la entrada al editor y en S3 gana
el boton de pinear."
```

---
## Task 4: El motor Direct2D sobre el dispositivo de la captura

Primer código de `pixpin-render`. Direct2D dibuja sobre el **mismo** `ID3D11Device` que posee la textura capturada: así el bitmap del fondo del overlay es la textura misma, sin copiarla.

**Regla de capas en acción:** `pixpin-render` (L1) recibe `&ID3D11Device` —tipo del crate `windows`— y nunca nombra a `pixpin-capture` (L2). El test de capas ignora los crates externos a propósito; el cableado real lo hace `apps/pixpin`.

**Files:**
- Create: `crates/pixpin-render/src/motor.rs`
- Modify: `crates/pixpin-render/src/lib.rs`, `crates/pixpin-render/Cargo.toml`

**Interfaces:**
- Consumes: nada interno.
- Produces:
  - `pub struct MotorRender { … }` con `pub fn nuevo(d3d: &ID3D11Device) -> Result<Self, ErrorRender>; pub fn contexto(&self) -> &ID2D1DeviceContext; pub fn dwrite(&self) -> &IDWriteFactory; pub fn bitmap_desde_textura(&self, t: &ID3D11Texture2D) -> Result<ID2D1Bitmap1, ErrorRender>; pub fn destino_desde_textura(&self, t: &ID3D11Texture2D) -> Result<ID2D1Bitmap1, ErrorRender>`
  - `pub struct Color { pub r: f32, pub g: f32, pub b: f32, pub a: f32 }` con constantes `NEGRO`, `BLANCO`, `ACENTO` y `pub fn oscurecido() -> Color` (negro al 55 %)
  - `pub enum ErrorRender { Windows(windows::core::Error), SinDxgi }`

- [ ] **Step 1: Añadir dependencias**

```bash
cargo add windows -p pixpin-render --features Win32_Foundation,Win32_Graphics_Direct2D,Win32_Graphics_Direct2D_Common,Win32_Graphics_Direct3D,Win32_Graphics_Direct3D11,Win32_Graphics_Dxgi,Win32_Graphics_Dxgi_Common,Win32_Graphics_DirectWrite,Win32_Graphics_DirectComposition,Win32_Graphics_Gdi,Win32_UI_WindowsAndMessaging,Win32_System_LibraryLoader
cargo add thiserror -p pixpin-render
```

**Si alguna característica no existe con ese nombre, ajústala y repórtalo** — en S1-B1 pasó tres veces y ninguna fue un problema, pero todas quedaron escritas.

Run: `cargo test -p pixpin --test capas -- --test-threads=1`
Expected: PASS — `pixpin-render` no ganó ninguna dependencia interna.

- [ ] **Step 2: Escribir el test que falla**

Crear `crates/pixpin-render/src/motor.rs` con sólo las pruebas:

```rust
#[cfg(test)]
mod pruebas {
    use super::*;
    use windows::Win32::Graphics::Direct3D::D3D_DRIVER_TYPE_HARDWARE;
    use windows::Win32::Graphics::Direct3D11::{
        D3D11_BIND_RENDER_TARGET, D3D11_BIND_SHADER_RESOURCE, D3D11_CPU_ACCESS_READ,
        D3D11_CREATE_DEVICE_BGRA_SUPPORT, D3D11_MAP_READ, D3D11_MAPPED_SUBRESOURCE,
        D3D11_SDK_VERSION, D3D11_TEXTURE2D_DESC, D3D11_USAGE_DEFAULT, D3D11_USAGE_STAGING,
        D3D11CreateDevice, ID3D11Device, ID3D11DeviceContext, ID3D11Texture2D,
    };
    use windows::Win32::Graphics::Dxgi::Common::{DXGI_FORMAT_B8G8R8A8_UNORM, DXGI_SAMPLE_DESC};

    /// Un dispositivo D3D11 minimo, solo para el test. No se usa el de
    /// pixpin-capture porque render (L1) no puede depender de capture (L2)
    /// ni siquiera en dev-dependencies: el test de capas tambien las mira.
    fn dispositivo_de_prueba() -> (ID3D11Device, ID3D11DeviceContext) {
        let mut d3d = None;
        let mut ctx = None;
        // SAFETY: punteros de salida locales inicializados a None; constantes
        // documentadas en el resto de parametros.
        unsafe {
            D3D11CreateDevice(
                None,
                D3D_DRIVER_TYPE_HARDWARE,
                Default::default(),
                D3D11_CREATE_DEVICE_BGRA_SUPPORT,
                None,
                D3D11_SDK_VERSION,
                Some(&mut d3d),
                None,
                Some(&mut ctx),
            )
            .expect("el test necesita GPU real");
        }
        (d3d.unwrap(), ctx.unwrap())
    }

    fn textura(d3d: &ID3D11Device, ancho: u32, alto: u32) -> ID3D11Texture2D {
        let desc = D3D11_TEXTURE2D_DESC {
            Width: ancho,
            Height: alto,
            MipLevels: 1,
            ArraySize: 1,
            Format: DXGI_FORMAT_B8G8R8A8_UNORM,
            SampleDesc: DXGI_SAMPLE_DESC { Count: 1, Quality: 0 },
            Usage: D3D11_USAGE_DEFAULT,
            BindFlags: (D3D11_BIND_SHADER_RESOURCE.0 | D3D11_BIND_RENDER_TARGET.0) as u32,
            ..Default::default()
        };
        let mut t = None;
        // SAFETY: desc inicializada; t es la variable de salida local.
        unsafe { d3d.CreateTexture2D(&desc, None, Some(&mut t)) }.unwrap();
        t.unwrap()
    }

    /// Lee un pixel BGRA de una textura, via staging.
    fn pixel(d3d: &ID3D11Device, ctx: &ID3D11DeviceContext, t: &ID3D11Texture2D, x: u32, y: u32) -> [u8; 4] {
        let mut desc = D3D11_TEXTURE2D_DESC::default();
        // SAFETY: GetDesc solo rellena la estructura local.
        unsafe { t.GetDesc(&mut desc) };
        let escenificada_desc = D3D11_TEXTURE2D_DESC {
            Usage: D3D11_USAGE_STAGING,
            BindFlags: 0,
            CPUAccessFlags: D3D11_CPU_ACCESS_READ.0 as u32,
            MiscFlags: 0,
            ..desc
        };
        let mut e = None;
        // SAFETY: desc inicializada; e es la salida local.
        unsafe { d3d.CreateTexture2D(&escenificada_desc, None, Some(&mut e)) }.unwrap();
        let e = e.unwrap();
        // SAFETY: mismo formato y tamano, usos compatibles con CopyResource.
        unsafe { ctx.CopyResource(&e, t) };
        let mut mapa = D3D11_MAPPED_SUBRESOURCE::default();
        // SAFETY: textura staging con lectura; se desmapea justo despues.
        unsafe { ctx.Map(&e, 0, D3D11_MAP_READ, 0, Some(&mut mapa)) }.unwrap();
        let paso = mapa.RowPitch as usize;
        // SAFETY: pData apunta a alto*paso bytes mientras este mapeada y el
        // offset leido queda dentro por construccion (x,y dentro de la textura,
        // obligacion del llamante de este helper de test).
        let v = unsafe {
            let base = (mapa.pData as *const u8).add(y as usize * paso + x as usize * 4);
            [*base, *base.add(1), *base.add(2), *base.add(3)]
        };
        // SAFETY: se desmapea lo que se mapeo, una vez.
        unsafe { ctx.Unmap(&e, 0) };
        v
    }

    #[test]
    #[ignore = "necesita GPU real; ejecutar con --ignored"]
    fn dibujar_un_rectangulo_rojo_deja_pixeles_rojos_en_la_textura() {
        let (d3d, ctx) = dispositivo_de_prueba();
        let motor = MotorRender::nuevo(&d3d).expect("deberia crearse el motor D2D");
        let destino = textura(&d3d, 64, 64);
        let objetivo = motor.destino_desde_textura(&destino).unwrap();

        let c = motor.contexto();
        // SAFETY: el contexto D2D esta vivo (lo mantiene `motor`) y las
        // llamadas siguen el protocolo SetTarget/BeginDraw/EndDraw.
        unsafe {
            c.SetTarget(&objetivo);
            c.BeginDraw();
            c.Clear(Some(&Color { r: 0.0, g: 0.0, b: 1.0, a: 1.0 }.a_d2d()));
            let pincel = c
                .CreateSolidColorBrush(&Color { r: 1.0, g: 0.0, b: 0.0, a: 1.0 }.a_d2d(), None)
                .unwrap();
            c.FillRectangle(
                &windows::Win32::Graphics::Direct2D::Common::D2D_RECT_F {
                    left: 8.0,
                    top: 8.0,
                    right: 32.0,
                    bottom: 32.0,
                },
                &pincel,
            );
            c.EndDraw(None, None).unwrap();
            c.SetTarget(None);
        }

        // Dentro del rectangulo: rojo (BGRA = 0,0,255). Fuera: azul.
        assert_eq!(pixel(&d3d, &ctx, &destino, 16, 16), [0, 0, 255, 255]);
        // Caso negativo: si Clear no funcionara o el rect lo cubriera todo,
        // este pixel tambien seria rojo.
        assert_eq!(pixel(&d3d, &ctx, &destino, 50, 50), [255, 0, 0, 255]);
    }

    #[test]
    #[ignore = "necesita GPU real; ejecutar con --ignored"]
    fn un_bitmap_de_solo_lectura_envuelve_la_textura_sin_copiarla() {
        let (d3d, _ctx) = dispositivo_de_prueba();
        let motor = MotorRender::nuevo(&d3d).unwrap();
        let t = textura(&d3d, 32, 32);
        let bitmap = motor.bitmap_desde_textura(&t).expect("deberia envolverse");
        // SAFETY: GetSize es una lectura sin precondiciones sobre un bitmap vivo.
        let tam = unsafe { bitmap.GetSize() };
        assert_eq!((tam.width as u32, tam.height as u32), (32, 32));
    }
}
```

- [ ] **Step 3: Ejecutar y comprobar que falla**

Run: `cargo test -p pixpin-render --lib -- --test-threads=1 --ignored`
Expected: FAIL — no existe `MotorRender`.

- [ ] **Step 4: Implementar**

Añadir al principio de `crates/pixpin-render/src/motor.rs`:

```rust
//! El motor Direct2D: factorias, dispositivo y contexto sobre el D3D11 de
//! la captura.
//!
//! La decision que sostiene esta fase: D2D dibuja sobre el MISMO dispositivo
//! D3D11 que posee la textura capturada. Asi el fondo del overlay es la
//! textura misma envuelta en un bitmap, sin copia y sin bajar a la CPU. Si
//! fueran dispositivos distintos, harian falta texturas compartidas y
//! sincronizacion — complejidad que no compra nada.
//!
//! `pixpin-render` es L1: recibe `&ID3D11Device` y no sabe de donde sale.

use windows::Win32::Graphics::Direct2D::Common::{
    D2D1_ALPHA_MODE_IGNORE, D2D1_ALPHA_MODE_PREMULTIPLIED, D2D1_COLOR_F, D2D1_PIXEL_FORMAT,
};
use windows::Win32::Graphics::Direct2D::{
    D2D1_BITMAP_OPTIONS_NONE, D2D1_BITMAP_OPTIONS_TARGET, D2D1_BITMAP_PROPERTIES1,
    D2D1_DEVICE_CONTEXT_OPTIONS_NONE, D2D1_FACTORY_TYPE_SINGLE_THREADED, D2D1CreateFactory,
    ID2D1Bitmap1, ID2D1Device, ID2D1DeviceContext, ID2D1Factory1,
};
use windows::Win32::Graphics::Direct3D11::{ID3D11Device, ID3D11Texture2D};
use windows::Win32::Graphics::DirectWrite::{
    DWRITE_FACTORY_TYPE_SHARED, DWriteCreateFactory, IDWriteFactory,
};
use windows::Win32::Graphics::Dxgi::Common::DXGI_FORMAT_B8G8R8A8_UNORM;
use windows::Win32::Graphics::Dxgi::{IDXGIDevice, IDXGISurface};
use windows::core::Interface;

#[derive(Debug, thiserror::Error)]
pub enum ErrorRender {
    #[error("error de Windows al dibujar: {0}")]
    Windows(#[from] windows::core::Error),
    #[error("la textura no expone una superficie DXGI")]
    SinDxgi,
}

/// Color RGBA en coma flotante 0..1, el idioma nativo de Direct2D.
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct Color {
    pub r: f32,
    pub g: f32,
    pub b: f32,
    pub a: f32,
}

impl Color {
    pub const NEGRO: Color = Color { r: 0.0, g: 0.0, b: 0.0, a: 1.0 };
    pub const BLANCO: Color = Color { r: 1.0, g: 1.0, b: 1.0, a: 1.0 };
    /// El azul de acento de la seleccion.
    pub const ACENTO: Color = Color { r: 0.13, g: 0.55, b: 0.95, a: 1.0 };

    /// El velo que oscurece lo no seleccionado.
    pub fn oscurecido() -> Color {
        Color { r: 0.0, g: 0.0, b: 0.0, a: 0.55 }
    }

    pub fn a_d2d(self) -> D2D1_COLOR_F {
        D2D1_COLOR_F { r: self.r, g: self.g, b: self.b, a: self.a }
    }
}

pub struct MotorRender {
    _fabrica: ID2D1Factory1,
    _dispositivo: ID2D1Device,
    contexto: ID2D1DeviceContext,
    dwrite: IDWriteFactory,
}

impl MotorRender {
    pub fn nuevo(d3d: &ID3D11Device) -> Result<Self, ErrorRender> {
        // SAFETY: crear la factoria no tiene precondiciones; single-threaded
        // porque todo el dibujo ocurre en el hilo de interfaz.
        let fabrica: ID2D1Factory1 =
            unsafe { D2D1CreateFactory(D2D1_FACTORY_TYPE_SINGLE_THREADED, None)? };
        let dxgi: IDXGIDevice = d3d.cast().map_err(|_| ErrorRender::SinDxgi)?;
        // SAFETY: `dxgi` es una vista valida del dispositivo del llamante,
        // que debe mantenerlo vivo mientras viva este motor (lo garantiza el
        // conteo de referencias COM del propio cast).
        let dispositivo = unsafe { fabrica.CreateDevice(&dxgi)? };
        // SAFETY: el dispositivo D2D se acaba de crear y esta vivo.
        let contexto = unsafe { dispositivo.CreateDeviceContext(D2D1_DEVICE_CONTEXT_OPTIONS_NONE)? };
        // SAFETY: crear la factoria compartida de DirectWrite no tiene
        // precondiciones.
        let dwrite: IDWriteFactory = unsafe { DWriteCreateFactory(DWRITE_FACTORY_TYPE_SHARED)? };
        Ok(Self { _fabrica: fabrica, _dispositivo: dispositivo, contexto, dwrite })
    }

    pub fn contexto(&self) -> &ID2D1DeviceContext {
        &self.contexto
    }

    pub fn dwrite(&self) -> &IDWriteFactory {
        &self.dwrite
    }

    /// Envuelve una textura como bitmap de SOLO LECTURA para dibujarla.
    /// No copia pixeles: el bitmap ES la textura.
    pub fn bitmap_desde_textura(&self, t: &ID3D11Texture2D) -> Result<ID2D1Bitmap1, ErrorRender> {
        self.envolver(t, D2D1_BITMAP_OPTIONS_NONE.0)
    }

    /// Envuelve una textura como DESTINO de dibujo (`SetTarget`).
    pub fn destino_desde_textura(&self, t: &ID3D11Texture2D) -> Result<ID2D1Bitmap1, ErrorRender> {
        self.envolver(t, D2D1_BITMAP_OPTIONS_TARGET.0)
    }

    fn envolver(&self, t: &ID3D11Texture2D, opciones: i32) -> Result<ID2D1Bitmap1, ErrorRender> {
        let superficie: IDXGISurface = t.cast().map_err(|_| ErrorRender::SinDxgi)?;
        let propiedades = D2D1_BITMAP_PROPERTIES1 {
            pixelFormat: D2D1_PIXEL_FORMAT {
                format: DXGI_FORMAT_B8G8R8A8_UNORM,
                // La captura no trae alfa util; ignorarlo evita que el velo
                // se componga contra basura del canal alfa.
                alphaMode: if opciones == D2D1_BITMAP_OPTIONS_TARGET.0 {
                    D2D1_ALPHA_MODE_PREMULTIPLIED
                } else {
                    D2D1_ALPHA_MODE_IGNORE
                },
            },
            dpiX: 96.0,
            dpiY: 96.0,
            bitmapOptions: windows::Win32::Graphics::Direct2D::D2D1_BITMAP_OPTIONS(opciones),
            colorContext: std::mem::ManuallyDrop::new(None),
        };
        // SAFETY: la superficie procede de la textura del llamante, viva
        // durante la llamada; D2D retiene su propia referencia al crearla.
        let bitmap = unsafe {
            self.contexto.CreateBitmapFromDxgiSurface(&superficie, Some(&propiedades))?
        };
        Ok(bitmap)
    }
}
```

Reemplazar `crates/pixpin-render/src/lib.rs`:

```rust
//! pixpin-render — el backend de dibujo Direct2D + DirectWrite.
//!
//! Frontera unica de dibujo del proyecto: todo lo que se pinta pasa por
//! aqui. Este crate habla con el sistema; `unsafe` permitido con `// SAFETY:`
//! en cada bloque. El bucle de render es dirigido por eventos, nunca por
//! fotogramas: sin trabajo no se dibuja nada, y de ahi sale el 0% de CPU.
#![deny(clippy::undocumented_unsafe_blocks)]

pub mod motor;

pub use motor::{Color, ErrorRender, MotorRender};
```

- [ ] **Step 5: Ejecutar y comprobar que pasa**

Run: `cargo test -p pixpin-render --lib -- --test-threads=1 --ignored`
Expected: PASS — el píxel rojo dentro, el azul fuera, y el bitmap envolvente con las medidas de la textura.

**Nota sobre las firmas:** `D2D1_BITMAP_OPTIONS`, `ManuallyDrop` en `colorContext` y los `unsafe` de los métodos COM varían entre versiones del crate `windows`. Ajusta mecánicamente y **repórtalo**; si algo exige decidir en vez de ajustar, párate.

- [ ] **Step 6: Comprobar el workspace y confirmar**

Run: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace -- --test-threads=1 && cargo deny check licenses bans sources`

```bash
git add crates/pixpin-render Cargo.lock
git commit -m "Motor Direct2D sobre el dispositivo D3D11 de la captura

D2D dibuja sobre el MISMO dispositivo que posee la textura capturada: el
fondo del overlay es la textura envuelta en un bitmap, sin copia y sin
bajar a la CPU. pixpin-render es L1 y recibe &ID3D11Device sin saber de
donde sale; quien cablea captura y render es el ejecutable.

El test de humo dibuja un rectangulo rojo sobre fondo azul y lee ambos
pixeles por staging: si el orden de canales, el alfa o el SetTarget
estuvieran mal, se ve aqui y no en el primer overlay."
```

---

## Task 5: La superficie de composición por ventana

DirectComposition más un swapchain de composición por HWND. Es lo que permite `WS_EX_NOREDIRECTIONBITMAP`: la ventana no tiene superficie GDI; lo único que existe es lo que el swapchain presenta.

**Files:**
- Create: `crates/pixpin-render/src/superficie.rs`
- Modify: `crates/pixpin-render/src/lib.rs`

**Interfaces:**
- Consumes: `MotorRender`, `ErrorRender` (Task 4).
- Produces:
  - `pub struct Superficie { … }` con `pub fn nueva(motor: &MotorRender, d3d: &ID3D11Device, hwnd: HWND, ancho: u32, alto: u32) -> Result<Self, ErrorRender>; pub fn empezar(&self, motor: &MotorRender) -> Result<ID2D1Bitmap1, ErrorRender>` (devuelve el backbuffer envuelto como destino; el llamante hace `SetTarget`+`BeginDraw`) `; pub fn presentar(&self) -> Result<(), ErrorRender>`

- [ ] **Step 1: Escribir el test que falla**

Crear `crates/pixpin-render/src/superficie.rs` con sólo las pruebas:

```rust
#[cfg(test)]
mod pruebas {
    use super::*;
    use crate::motor::{Color, MotorRender};
    use windows::Win32::Graphics::Direct3D::D3D_DRIVER_TYPE_HARDWARE;
    use windows::Win32::Graphics::Direct3D11::{
        D3D11_CREATE_DEVICE_BGRA_SUPPORT, D3D11_SDK_VERSION, D3D11CreateDevice, ID3D11Device,
    };
    use windows::Win32::System::LibraryLoader::GetModuleHandleW;
    use windows::Win32::UI::WindowsAndMessaging::{
        CW_USEDEFAULT, CreateWindowExW, DestroyWindow, HWND_MESSAGE, WINDOW_EX_STYLE, WS_POPUP,
    };
    use windows::core::w;

    fn d3d() -> ID3D11Device {
        let mut d = None;
        // SAFETY: igual que en motor.rs: salidas locales, constantes documentadas.
        unsafe {
            D3D11CreateDevice(
                None,
                D3D_DRIVER_TYPE_HARDWARE,
                Default::default(),
                D3D11_CREATE_DEVICE_BGRA_SUPPORT,
                None,
                D3D11_SDK_VERSION,
                Some(&mut d),
                None,
                None,
            )
            .expect("GPU real");
        }
        d.unwrap()
    }

    #[test]
    #[ignore = "necesita GPU y sesion de escritorio; ejecutar con --ignored"]
    fn se_crea_se_dibuja_y_se_presenta_sin_error() {
        // Una ventana minima descartable. La clase "STATIC" del sistema
        // evita registrar una propia solo para el test.
        // SAFETY: CreateWindowExW con la clase del sistema no exige mas que
        // un modulo valido; la ventana se destruye al final del test.
        let hwnd = unsafe {
            CreateWindowExW(
                WINDOW_EX_STYLE(0),
                w!("STATIC"),
                w!("prueba"),
                WS_POPUP,
                CW_USEDEFAULT,
                CW_USEDEFAULT,
                256,
                256,
                None,
                None,
                Some(GetModuleHandleW(None).unwrap().into()),
                None,
            )
            .expect("ventana de prueba")
        };

        let d3d = d3d();
        let motor = MotorRender::nuevo(&d3d).unwrap();
        let superficie = Superficie::nueva(&motor, &d3d, hwnd, 256, 256)
            .expect("deberia crearse la superficie de composicion");

        let destino = superficie.empezar(&motor).unwrap();
        let c = motor.contexto();
        // SAFETY: protocolo SetTarget/BeginDraw/EndDraw sobre objetos vivos.
        unsafe {
            c.SetTarget(&destino);
            c.BeginDraw();
            c.Clear(Some(&Color::ACENTO.a_d2d()));
            c.EndDraw(None, None).unwrap();
            c.SetTarget(None);
        }
        superficie.presentar().expect("present deberia funcionar");

        // Dos fotogramas seguidos: el swapchain rota los buffers y el
        // segundo present fallaria si empezar() no re-envolviera el
        // backbuffer actual. Es el caso negativo del diseno de un solo uso.
        let destino2 = superficie.empezar(&motor).unwrap();
        // SAFETY: igual que arriba.
        unsafe {
            c.SetTarget(&destino2);
            c.BeginDraw();
            c.Clear(Some(&Color::NEGRO.a_d2d()));
            c.EndDraw(None, None).unwrap();
            c.SetTarget(None);
        }
        superficie.presentar().expect("el segundo present tambien");

        // SAFETY: la ventana la creo este test y nadie mas la usa.
        unsafe { DestroyWindow(hwnd).unwrap() };
        let _ = HWND_MESSAGE; // silencia el import si la version del crate no lo usa
    }
}
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `cargo test -p pixpin-render --lib superficie -- --test-threads=1 --ignored`
Expected: FAIL — no existe `Superficie`.

- [ ] **Step 3: Implementar**

Añadir al principio de `crates/pixpin-render/src/superficie.rs`:

```rust
//! Una superficie de composicion por ventana de overlay.
//!
//! `WS_EX_NOREDIRECTIONBITMAP` quita a la ventana su superficie GDI: lo
//! unico que se ve es lo que este swapchain presenta a traves de
//! DirectComposition. Es el camino sin parpadeo y sin copias del escritorio
//! moderno, el mismo que usa QuickView — la tecnica, no el codigo.
//!
//! El present es A DEMANDA: se presenta cuando algo cambio, nunca en un
//! bucle de fotogramas. De ahi sale el 0% de CPU en reposo del overlay.

use windows::Win32::Foundation::HWND;
use windows::Win32::Graphics::Direct2D::ID2D1Bitmap1;
use windows::Win32::Graphics::Direct3D11::ID3D11Device;
use windows::Win32::Graphics::DirectComposition::{
    DCompositionCreateDevice, IDCompositionDevice, IDCompositionTarget, IDCompositionVisual,
};
use windows::Win32::Graphics::Dxgi::Common::{
    DXGI_ALPHA_MODE_PREMULTIPLIED, DXGI_FORMAT_B8G8R8A8_UNORM, DXGI_SAMPLE_DESC,
};
use windows::Win32::Graphics::Dxgi::{
    DXGI_SCALING_STRETCH, DXGI_SWAP_CHAIN_DESC1, DXGI_SWAP_EFFECT_FLIP_SEQUENTIAL,
    DXGI_USAGE_RENDER_TARGET_OUTPUT, IDXGIDevice, IDXGIFactory2, IDXGISwapChain1,
};
use windows::Win32::Graphics::Direct3D11::ID3D11Texture2D;
use windows::core::Interface;

use crate::motor::{ErrorRender, MotorRender};

pub struct Superficie {
    _dcomp: IDCompositionDevice,
    _objetivo: IDCompositionTarget,
    _visual: IDCompositionVisual,
    swapchain: IDXGISwapChain1,
}

impl Superficie {
    pub fn nueva(
        _motor: &MotorRender,
        d3d: &ID3D11Device,
        hwnd: HWND,
        ancho: u32,
        alto: u32,
    ) -> Result<Self, ErrorRender> {
        let dxgi: IDXGIDevice = d3d.cast().map_err(|_| ErrorRender::SinDxgi)?;
        // SAFETY: el adaptador y la factoria se obtienen del dispositivo del
        // llamante, vivo durante toda la llamada.
        let fabrica: IDXGIFactory2 = unsafe { dxgi.GetAdapter()?.GetParent()? };

        let desc = DXGI_SWAP_CHAIN_DESC1 {
            Width: ancho,
            Height: alto,
            Format: DXGI_FORMAT_B8G8R8A8_UNORM,
            SampleDesc: DXGI_SAMPLE_DESC { Count: 1, Quality: 0 },
            BufferUsage: DXGI_USAGE_RENDER_TARGET_OUTPUT,
            // Dos buffers y flip: el minimo que permite componer sin copia.
            BufferCount: 2,
            SwapEffect: DXGI_SWAP_EFFECT_FLIP_SEQUENTIAL,
            Scaling: DXGI_SCALING_STRETCH,
            AlphaMode: DXGI_ALPHA_MODE_PREMULTIPLIED,
            ..Default::default()
        };
        // SAFETY: swapchain DE COMPOSICION (sin HWND propio): es el unico
        // tipo valido para NOREDIRECTIONBITMAP; parametros documentados.
        let swapchain =
            unsafe { fabrica.CreateSwapChainForComposition(&dxgi, &desc, None)? };

        // SAFETY: dispositivo de composicion sobre el mismo DXGI; el target
        // toma la ventana del llamante, que debe sobrevivir a la superficie
        // (obligacion del llamante: la ventana posee a su Superficie).
        let dcomp: IDCompositionDevice = unsafe { DCompositionCreateDevice(&dxgi)? };
        let objetivo = unsafe { dcomp.CreateTargetForHwnd(hwnd, true)? };
        let visual = unsafe { dcomp.CreateVisual()? };
        // SAFETY: los tres objetos se acaban de crear y estan vivos; Commit
        // publica el arbol.
        unsafe {
            visual.SetContent(&swapchain)?;
            objetivo.SetRoot(&visual)?;
            dcomp.Commit()?;
        }

        Ok(Self { _dcomp: dcomp, _objetivo: objetivo, _visual: visual, swapchain })
    }

    /// El backbuffer ACTUAL envuelto como destino D2D. Hay que volver a
    /// llamarlo en cada fotograma: con flip, el backbuffer rota en cada
    /// present y el bitmap anterior queda apuntando al buffer equivocado.
    ///
    /// Correccion de ronda 1 (descubierta ejecutando, no leyendo): los
    /// buferes de un swapchain exigen TARGET | CANNOT_DRAW y alfa
    /// premultiplicado; envolverlos solo como TARGET da E_INVALIDARG. El
    /// motor gana `destino_backbuffer` con esas opciones, y la rama del
    /// alfa en `envolver` comprueba el bit TARGET, no la igualdad exacta.
    pub fn empezar(&self, motor: &MotorRender) -> Result<ID2D1Bitmap1, ErrorRender> {
        // SAFETY: indice 0 es siempre el backbuffer escribible actual.
        let textura: ID3D11Texture2D = unsafe { self.swapchain.GetBuffer(0)? };
        motor.destino_backbuffer(&textura)
    }

    pub fn presentar(&self) -> Result<(), ErrorRender> {
        // SAFETY: present sin espera de vsync (0,0): el bucle es dirigido
        // por eventos y no debe bloquear el hilo de interfaz.
        unsafe { self.swapchain.Present(0, Default::default()).ok()? };
        Ok(())
    }
}
```

Añadir a `crates/pixpin-render/src/lib.rs`:

```rust
pub mod superficie;

pub use superficie::Superficie;
```

- [ ] **Step 4: Ejecutar y comprobar que pasa**

Run: `cargo test -p pixpin-render --lib -- --test-threads=1 --ignored`
Expected: PASS — los dos fotogramas presentados.

- [ ] **Step 5: Comprobar el workspace y confirmar**

Run: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace -- --test-threads=1`

```bash
git add crates/pixpin-render
git commit -m "Superficie de composicion por ventana: DComp + swapchain flip

WS_EX_NOREDIRECTIONBITMAP quita a la ventana su superficie GDI: lo unico
visible es lo que el swapchain de composicion presenta. Present a demanda,
nunca en bucle: de ahi sale el 0% de CPU del overlay en reposo.

empezar() re-envuelve el backbuffer en cada fotograma porque con flip el
buffer rota en cada present; el test dibuja y presenta DOS veces porque un
diseno de un solo uso pasaria el primer fotograma y fallaria el segundo."
```

---
## Task 6: La ventana de overlay

Una por monitor, con **su propia clase y su propio `WndProc`** — la retrospectiva de S1-A lo exige: el `WndProc` de `VentanaMensajes` llama a `PostQuitMessage` ante cualquier `WM_DESTROY`, y si los overlays lo compartieran, cerrar uno mataría la aplicación.

**Files:**
- Create: `crates/pixpin-shell/src/overlay.rs`
- Modify: `crates/pixpin-shell/src/lib.rs`, `crates/pixpin-shell/Cargo.toml`

**Interfaces:**
- Consumes: `pixpin_geom::{Punto, Rect}` (ya es dependencia vía `pixpin-nivel`? No: añadir `pixpin-geom` a `pixpin-shell` — L1→L0, legal).
- Produces:
  - `pub enum EventoOverlay { RatonMovido(Punto), BotonPulsado(Punto), BotonSoltado(Punto), Tecla { vk: u32, shift: bool }, Pintar, CambioDpi, Despierta }` (los `Punto` en **coordenadas físicas del escritorio virtual**, no de la ventana; `Despierta` es la traducción de `MSG_DESPIERTA`)
  - `pub enum FormaCursorWin { Cruz, Mover, RedimNS, RedimEO, RedimNeSo, RedimNoSe }`
  - `pub struct VentanaOverlay { … }` con `pub fn nueva(area: Rect) -> Result<Self, ErrorOverlay>; pub fn handle(&self) -> HWND; pub fn area(&self) -> Rect; pub fn mostrar(&self); pub fn invalidar(&self); pub fn poner_cursor(&self, f: FormaCursorWin)`
  - `pub fn bucle_modal(ventanas: &[VentanaOverlay], mut callback: impl FnMut(HWND, EventoOverlay) -> Continuar)` — bombea mensajes hasta que el callback devuelva `Continuar::No`. Reutiliza `Continuar` de `ventana.rs`.
  - `pub const MSG_DESPIERTA: u32` — mensaje `WM_APP+1` que otros hilos pueden `PostMessage` para despertar el bucle (lo usa la sesión viva de la Task 8 y el hilo UIA de la Task 7).

**Decisiones de implementación que el ejecutor debe respetar:**

- Estilos: `WS_POPUP` · extendidos: `WS_EX_TOPMOST | WS_EX_NOREDIRECTIONBITMAP | WS_EX_TOOLWINDOW` (sin botón en la barra de tareas).
- La clase se registra una vez (`Once`) con nombre `PixPinOverlay`, cursor nulo (lo gestiona `poner_cursor` respondiendo a `WM_SETCURSOR`).
- El `WndProc` **no** llama a `PostQuitMessage` jamás. Traduce: `WM_MOUSEMOVE`/`WM_LBUTTONDOWN`/`WM_LBUTTONUP` (con `SetCapture`/`ReleaseCapture` para no perder el arrastre al salir de la ventana), `WM_KEYDOWN` (vk + estado de Shift vía `GetKeyState`), `WM_PAINT` (→ `Pintar` + `ValidateRect`), `WM_DPICHANGED` (→ `CambioDpi`).
- Los eventos se entregan por una cola `thread_local` (`RefCell<VecDeque<(HWND, EventoOverlay)>>`) que `bucle_modal` drena tras cada `DispatchMessageW` — mismo patrón `PENDIENTES` de S1-A, correcto aquí porque el `WndProc` corre en el hilo de interfaz. Los hilos ajenos **no** tocan esta cola: usan `PostMessage(MSG_DESPIERTA)`, que llega como mensaje normal y el bucle lo traduce.
- Las coordenadas de ratón llegan en cliente: se convierten a escritorio virtual sumando el origen del `area` de la ventana.
- `Drop` destruye la ventana con `DestroyWindow` si sigue viva.

- [ ] **Step 1: Añadir la dependencia y escribir los tests que fallan**

```bash
cargo add pixpin-geom --path crates/pixpin-geom -p pixpin-shell
```

Crear `crates/pixpin-shell/src/overlay.rs` con sólo las pruebas:

```rust
#[cfg(test)]
mod pruebas {
    use super::*;
    use pixpin_geom::Rect;

    #[test]
    #[ignore = "necesita sesion de escritorio; ejecutar con --ignored"]
    fn crear_y_destruir_un_overlay_no_toca_al_resto() {
        // La mina de la retrospectiva: si el WndProc del overlay llamara a
        // PostQuitMessage en WM_DESTROY, destruir la primera ventana
        // contaminaria el bucle de la segunda. Se destruye una y se
        // comprueba que la otra sigue viva y funcional.
        let a = VentanaOverlay::nueva(Rect { x: 0, y: 0, ancho: 200, alto: 200 })
            .expect("primera ventana");
        let b = VentanaOverlay::nueva(Rect { x: 200, y: 0, ancho: 200, alto: 200 })
            .expect("segunda ventana");

        drop(a);

        // SAFETY: IsWindow es una consulta sin precondiciones.
        let viva = unsafe {
            windows::Win32::UI::WindowsAndMessaging::IsWindow(Some(b.handle())).as_bool()
        };
        assert!(viva, "destruir un overlay no debe llevarse a los demas");
    }

    #[test]
    #[ignore = "necesita sesion de escritorio; ejecutar con --ignored"]
    fn el_bucle_modal_termina_cuando_el_callback_dice_no() {
        let v = VentanaOverlay::nueva(Rect { x: 0, y: 0, ancho: 100, alto: 100 }).unwrap();
        // Se autoenvia el mensaje de despertar; el callback corta al verlo.
        // Si bucle_modal no drenara la cola ni tradujera MSG_DESPIERTA, este
        // test se quedaria colgado (el timeout del harness lo delata).
        // SAFETY: PostMessageW a una ventana propia y viva.
        unsafe {
            windows::Win32::UI::WindowsAndMessaging::PostMessageW(
                Some(v.handle()),
                MSG_DESPIERTA,
                Default::default(),
                Default::default(),
            )
            .unwrap();
        }
        let mut despertares = 0;
        bucle_modal(std::slice::from_ref(&v), |_hwnd, evento| {
            if matches!(evento, EventoOverlay::Pintar) {
                return crate::ventana::Continuar::Si;
            }
            despertares += 1;
            crate::ventana::Continuar::No
        });
        assert_eq!(despertares, 1);
    }
}
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `cargo test -p pixpin-shell overlay -- --test-threads=1 --ignored`
Expected: FAIL — no existe `VentanaOverlay`.

- [ ] **Step 3: Implementar**

El esqueleto completo (los detalles de traducción de mensajes siguen el patrón de `ventana.rs`, que ya está revisado — cópialo, no lo reinventes):

```rust
//! La ventana de overlay: una por monitor, clase y WndProc PROPIOS.
//!
//! La retrospectiva de S1-A lo exige: el WndProc de VentanaMensajes llama a
//! PostQuitMessage ante cualquier WM_DESTROY. Si los overlays lo
//! compartieran, cerrar uno mataria la aplicacion. Este WndProc no llama a
//! PostQuitMessage NUNCA: el bucle modal termina porque el callback lo dice,
//! no porque una ventana muera.
//!
//! WS_EX_NOREDIRECTIONBITMAP: la ventana no tiene superficie GDI; todo lo
//! visible lo presenta la Superficie de pixpin-render por DirectComposition.

use std::cell::RefCell;
use std::collections::VecDeque;
use std::sync::Once;

use pixpin_geom::{Punto, Rect};
use windows::Win32::Foundation::{HWND, LPARAM, LRESULT, WPARAM};
use windows::Win32::System::LibraryLoader::GetModuleHandleW;
use windows::Win32::UI::WindowsAndMessaging::*;
use windows::core::w;

/// WM_APP+1: otros hilos lo PostMessage-an para despertar el bucle modal.
pub const MSG_DESPIERTA: u32 = WM_APP + 1;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum EventoOverlay {
    RatonMovido(Punto),
    BotonPulsado(Punto),
    BotonSoltado(Punto),
    Tecla { vk: u32, shift: bool },
    Pintar,
    CambioDpi,
    /// MSG_DESPIERTA recibido: hay trabajo de otro hilo esperando.
    Despierta,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum FormaCursorWin {
    Cruz,
    Mover,
    RedimNS,
    RedimEO,
    RedimNeSo,
    RedimNoSe,
}

#[derive(Debug, thiserror::Error)]
pub enum ErrorOverlay {
    #[error("no se pudo crear la ventana de overlay: {0}")]
    Creacion(#[source] windows::core::Error),
}

thread_local! {
    /// Cola de eventos del hilo de interfaz. Solo la toca el WndProc (que
    /// corre en este hilo) y bucle_modal. Los hilos ajenos usan PostMessage.
    static PENDIENTES_OVERLAY: RefCell<VecDeque<(HWND, EventoOverlay)>> =
        const { RefCell::new(VecDeque::new()) };
    /// Cursor vigente por ventana; WM_SETCURSOR lo consulta.
    static CURSOR: RefCell<Vec<(HWND, FormaCursorWin)>> = const { RefCell::new(Vec::new()) };
}

static REGISTRO: Once = Once::new();

pub struct VentanaOverlay {
    hwnd: HWND,
    area: Rect,
}

impl VentanaOverlay {
    pub fn nueva(area: Rect) -> Result<Self, ErrorOverlay> {
        REGISTRO.call_once(|| registrar_clase());
        // SAFETY: la clase quedo registrada en call_once; los estilos son
        // constantes documentadas y el modulo es el propio.
        let hwnd = unsafe {
            CreateWindowExW(
                WS_EX_TOPMOST | WS_EX_NOREDIRECTIONBITMAP | WS_EX_TOOLWINDOW,
                w!("PixPinOverlay"),
                w!(""),
                WS_POPUP,
                area.x,
                area.y,
                area.ancho as i32,
                area.alto as i32,
                None,
                None,
                Some(GetModuleHandleW(None).map_err(ErrorOverlay::Creacion)?.into()),
                None,
            )
            .map_err(ErrorOverlay::Creacion)?
        };
        Ok(Self { hwnd, area })
    }

    pub fn handle(&self) -> HWND {
        self.hwnd
    }

    pub fn area(&self) -> Rect {
        self.area
    }

    pub fn mostrar(&self) {
        // SAFETY: la ventana es propia y esta viva; SW_SHOW no roba el foco
        // del resto de overlays porque cada uno se muestra una sola vez.
        unsafe {
            let _ = ShowWindow(self.hwnd, SW_SHOW);
        }
    }

    pub fn invalidar(&self) {
        // SAFETY: InvalidateRect sobre ventana propia; None invalida entera.
        unsafe {
            let _ = InvalidateRect(Some(self.hwnd), None, false);
        }
    }

    pub fn poner_cursor(&self, f: FormaCursorWin) {
        CURSOR.with(|c| {
            let mut c = c.borrow_mut();
            match c.iter_mut().find(|(h, _)| *h == self.hwnd) {
                Some(par) => par.1 = f,
                None => c.push((self.hwnd, f)),
            }
        });
    }
}

impl Drop for VentanaOverlay {
    fn drop(&mut self) {
        CURSOR.with(|c| c.borrow_mut().retain(|(h, _)| *h != self.hwnd));
        // SAFETY: destruir una ventana propia desde su hilo es valido; si ya
        // fue destruida por el sistema, DestroyWindow falla y se ignora.
        unsafe {
            let _ = DestroyWindow(self.hwnd);
        }
    }
}

/// Bombea mensajes hasta que el callback devuelva Continuar::No.
///
/// No usa GetMessage con filtro por ventana: el teclado y MSG_DESPIERTA
/// pueden llegar a cualquiera de los overlays.
pub fn bucle_modal(
    ventanas: &[VentanaOverlay],
    mut callback: impl FnMut(HWND, EventoOverlay) -> crate::ventana::Continuar,
) {
    let _ = ventanas; // las ventanas viven en el llamante; aqui solo se bombea
    let mut msg = MSG::default();
    loop {
        // SAFETY: GetMessageW con punteros locales validos; -1 es error.
        let r = unsafe { GetMessageW(&mut msg, None, 0, 0) };
        if r.0 <= 0 {
            return;
        }
        // SAFETY: protocolo estandar de bombeo.
        unsafe {
            let _ = TranslateMessage(&msg);
            DispatchMessageW(&msg);
        }
        // Drenar lo que el WndProc haya encolado durante el Dispatch.
        loop {
            let siguiente = PENDIENTES_OVERLAY.with(|p| p.borrow_mut().pop_front());
            let Some((hwnd, evento)) = siguiente else { break };
            if callback(hwnd, evento) == crate::ventana::Continuar::No {
                return;
            }
        }
    }
}

fn registrar_clase() {
    // SAFETY: registro unico (Once) de una clase con WndProc propio; los
    // campos no usados quedan a cero, que es lo que la API espera.
    unsafe {
        let clase = WNDCLASSW {
            lpfnWndProc: Some(procedimiento_overlay),
            hInstance: GetModuleHandleW(None).unwrap().into(),
            lpszClassName: w!("PixPinOverlay"),
            ..Default::default()
        };
        RegisterClassW(&clase);
    }
}

extern "system" fn procedimiento_overlay(
    hwnd: HWND,
    mensaje: u32,
    wparam: WPARAM,
    lparam: LPARAM,
) -> LRESULT {
    let encolar = |e: EventoOverlay| {
        PENDIENTES_OVERLAY.with(|p| p.borrow_mut().push_back((hwnd, e)));
    };
    // Cliente -> escritorio virtual: la ventana esta en area.x/area.y.
    let punto = |lparam: LPARAM| {
        // SAFETY: GetWindowRect sobre la ventana del propio WndProc.
        let mut r = windows::Win32::Foundation::RECT::default();
        unsafe {
            let _ = windows::Win32::UI::WindowsAndMessaging::GetWindowRect(hwnd, &mut r);
        }
        Punto {
            x: r.left + (lparam.0 & 0xFFFF) as i16 as i32,
            y: r.top + ((lparam.0 >> 16) & 0xFFFF) as i16 as i32,
        }
    };

    match mensaje {
        WM_MOUSEMOVE => {
            encolar(EventoOverlay::RatonMovido(punto(lparam)));
            LRESULT(0)
        }
        WM_LBUTTONDOWN => {
            // SAFETY: SetCapture sobre ventana propia: el arrastre no se
            // pierde al salir del borde.
            unsafe { SetCapture(hwnd) };
            encolar(EventoOverlay::BotonPulsado(punto(lparam)));
            LRESULT(0)
        }
        WM_LBUTTONUP => {
            // SAFETY: libera la captura tomada arriba.
            unsafe {
                let _ = ReleaseCapture();
            }
            encolar(EventoOverlay::BotonSoltado(punto(lparam)));
            LRESULT(0)
        }
        WM_KEYDOWN => {
            // SAFETY: GetKeyState es una consulta sin precondiciones.
            let shift = unsafe { GetKeyState(VK_SHIFT.0 as i32) } < 0;
            encolar(EventoOverlay::Tecla { vk: wparam.0 as u32, shift });
            LRESULT(0)
        }
        WM_PAINT => {
            // SAFETY: ValidateRect marca la ventana como pintada; el dibujo
            // real lo hace el swapchain, no GDI.
            unsafe {
                let _ = ValidateRect(Some(hwnd), None);
            }
            encolar(EventoOverlay::Pintar);
            LRESULT(0)
        }
        WM_DPICHANGED => {
            encolar(EventoOverlay::CambioDpi);
            LRESULT(0)
        }
        WM_SETCURSOR => {
            let forma = CURSOR.with(|c| {
                c.borrow().iter().find(|(h, _)| *h == hwnd).map(|(_, f)| *f)
            });
            let id = match forma.unwrap_or(FormaCursorWin::Cruz) {
                FormaCursorWin::Cruz => IDC_CROSS,
                FormaCursorWin::Mover => IDC_SIZEALL,
                FormaCursorWin::RedimNS => IDC_SIZENS,
                FormaCursorWin::RedimEO => IDC_SIZEWE,
                FormaCursorWin::RedimNeSo => IDC_SIZENESW,
                FormaCursorWin::RedimNoSe => IDC_SIZENWSE,
            };
            // SAFETY: LoadCursorW de un cursor del sistema y SetCursor son
            // llamadas sin precondiciones sobre recursos compartidos.
            unsafe {
                if let Ok(cursor) = LoadCursorW(None, id) {
                    SetCursor(Some(cursor));
                }
            }
            LRESULT(1)
        }
        m if m == MSG_DESPIERTA => {
            encolar(EventoOverlay::Despierta);
            LRESULT(0)
        }
        // NUNCA PostQuitMessage aqui: ver el comentario de modulo.
        _ => {
            // SAFETY: delegacion estandar al procedimiento por defecto.
            unsafe { DefWindowProcW(hwnd, mensaje, wparam, lparam) }
        }
    }
}
```

Añadir a `crates/pixpin-shell/src/lib.rs`:

```rust
pub mod overlay;
```

(sin `pub use` masivo: los consumidores nombran `pixpin_shell::overlay::VentanaOverlay`, que deja claro de qué familia de ventanas hablan.)

- [ ] **Step 4: Ejecutar y comprobar que pasa**

Run: `cargo test -p pixpin-shell overlay -- --test-threads=1 --ignored`
Expected: PASS — los dos tests, sin colgarse.

- [ ] **Step 5: Comprobar el workspace y confirmar**

Run: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace -- --test-threads=1`

```bash
git add crates/pixpin-shell Cargo.lock
git commit -m "Ventana de overlay con clase y WndProc propios

La mina que la retrospectiva de S1-A dejo marcada: el WndProc de
VentanaMensajes llama a PostQuitMessage ante cualquier WM_DESTROY, y si los
overlays lo compartieran, cerrar uno mataria la aplicacion. Este WndProc no
lo llama nunca, y el test destruye un overlay y comprueba que el otro sigue
vivo.

SetCapture en el boton para no perder el arrastre al cruzar el borde de la
ventana, coordenadas convertidas a escritorio virtual en la frontera, y
MSG_DESPIERTA (WM_APP+1) como unico canal por el que otros hilos despiertan
el bucle modal: la cola thread_local es del hilo de interfaz y de nadie mas."
```

---

## Task 7: El hilo de UI Automation que nunca bloquea

La regla de la spec: el overlay pregunta «¿qué hay bajo el cursor?» y recibe **de inmediato** lo último que se sepa. UIA es COM entre procesos y puede tardar decenas de milisegundos o colgarse; por eso vive en su hilo, con caché, y contesta por `PostMessage`.

**Files:**
- Create: `crates/pixpin-shell/src/uia.rs`
- Modify: `crates/pixpin-shell/src/lib.rs`, `crates/pixpin-shell/Cargo.toml`

**Interfaces:**
- Consumes: `pixpin_geom::{Punto, Rect, Candidato}`, `overlay::MSG_DESPIERTA` (Task 6).
- Produces:
  - `pub struct Uia { … }` con `pub fn nueva(notificar: HWND) -> Uia; pub fn pedir(&self, cursor: Punto); pub fn candidatos(&self) -> Vec<Candidato>; pub fn detener(self)`
  - Contrato: `pedir` **nunca bloquea** (canal con capacidad 1: si el hilo está ocupado, la petición vieja se descarta — solo importa la última posición). Cuando hay respuesta nueva, el hilo hace `PostMessage(notificar, MSG_DESPIERTA, …)` y `candidatos()` la devuelve al instante desde un `Mutex` que solo se toma para copiar el `Vec`.

**Qué produce el hilo por cada petición:**

1. **Siempre**, el respaldo: `WindowFromPoint` → ventana raíz (`GetAncestor GA_ROOT`) → `DwmGetWindowAttribute(DWMWA_EXTENDED_FRAME_BOUNDS)` como `Candidato` de profundidad 0. **Nunca `GetWindowRect`**: incluye la sombra y el recuadro saldría varios píxeles más grande por cada lado — la spec §2.5 lo marca como fallo conocido de la competencia. Se excluyen las propias ventanas `PixPinOverlay` (por nombre de clase) o el snap se ajustaría al overlay mismo.
2. Si UIA responde a tiempo: `IUIAutomation::ElementFromPoint` y la cadena de ancestros vía `IUIAutomationTreeWalker` (control view), cada uno con su `CurrentBoundingRectangle` como `Candidato` de profundidad creciente desde 1.
3. Caché por ventana raíz: si el cursor sigue sobre la misma raíz, se reutiliza el árbol y sólo cambia el respaldo; al cambiar de raíz se invalida.

- [ ] **Step 1: Añadir features y escribir los tests que fallan**

Añadir a la lista `features` de `windows` en `crates/pixpin-shell/Cargo.toml` (a mano):
`"Win32_UI_Accessibility"`, `"Win32_Graphics_Dwm"`.

Crear `crates/pixpin-shell/src/uia.rs` con sólo las pruebas:

```rust
#[cfg(test)]
mod pruebas {
    use super::*;
    use pixpin_geom::Punto;
    use std::time::{Duration, Instant};

    #[test]
    #[ignore = "necesita sesion de escritorio; ejecutar con --ignored"]
    fn pedir_no_bloquea_aunque_se_pida_en_rafaga() {
        // El contrato central: 200 peticiones seguidas deben costar
        // microsegundos cada una, este o no este el hilo UIA ocupado. Si
        // pedir() esperara la respuesta, esto tardaria segundos y el
        // overlay se congelaria igual que la competencia.
        let uia = Uia::nueva(HWND::default());
        let inicio = Instant::now();
        for i in 0..200 {
            uia.pedir(Punto { x: i, y: i });
        }
        assert!(
            inicio.elapsed() < Duration::from_millis(50),
            "pedir() esta bloqueando: {:?}",
            inicio.elapsed()
        );
        uia.detener();
    }

    #[test]
    #[ignore = "necesita sesion de escritorio; ejecutar con --ignored"]
    fn sobre_el_escritorio_siempre_hay_al_menos_el_respaldo() {
        // Puede no haber UIA (politicas, apps que no lo implementan), pero el
        // respaldo por ventana + DWM tiene que aparecer siempre.
        let uia = Uia::nueva(HWND::default());
        uia.pedir(Punto { x: 10, y: 10 });
        // Espera acotada: el hilo contesta o el test falla con lista vacia.
        let limite = Instant::now() + Duration::from_secs(3);
        let mut candidatos = Vec::new();
        while Instant::now() < limite {
            candidatos = uia.candidatos();
            if !candidatos.is_empty() {
                break;
            }
            std::thread::sleep(Duration::from_millis(20));
        }
        uia.detener();
        assert!(!candidatos.is_empty(), "ni siquiera llego el respaldo DWM");
        assert!(
            candidatos.iter().any(|c| c.profundidad == 0),
            "falta el candidato de profundidad 0 (la ventana raiz)"
        );
        assert!(
            candidatos.iter().all(|c| !c.rect.esta_vacio()),
            "ningun candidato puede tener area cero: {candidatos:?}"
        );
    }

    #[test]
    fn detener_no_se_cuelga_con_el_hilo_ocioso() {
        // No necesita escritorio: crea y detiene. Si detener() hiciera un
        // join sin cerrar el canal, este test se quedaria colgado.
        let uia = Uia::nueva(HWND::default());
        uia.detener();
    }
}
```

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `cargo test -p pixpin-shell uia -- --test-threads=1`
Expected: FAIL — no existe `Uia`.

- [ ] **Step 3: Implementar**

Estructura (el detalle COM de UIA sigue la documentación del crate; el esqueleto fija los contratos):

```rust
//! El hilo de UI Automation: candidatos de snap con cache, sin bloquear jamas.
//!
//! UIA es COM entre procesos: una consulta puede tardar decenas de
//! milisegundos o colgarse si la aplicacion destino no responde. La regla de
//! la spec: el overlay NUNCA espera. pedir() deja la posicion en un canal de
//! capacidad 1 (la nueva pisa a la vieja); el hilo contesta cuando puede,
//! deja el resultado bajo un Mutex breve y despierta al bucle modal con
//! PostMessage(MSG_DESPIERTA).

use std::sync::mpsc::{Receiver, SyncSender, TrySendError, sync_channel};
use std::sync::{Arc, Mutex};
use std::thread::JoinHandle;

use pixpin_geom::{Candidato, Punto, Rect};
use windows::Win32::Foundation::{HWND, RECT};
use windows::Win32::Graphics::Dwm::{DWMWA_EXTENDED_FRAME_BOUNDS, DwmGetWindowAttribute};
use windows::Win32::System::Com::{COINIT_MULTITHREADED, CoCreateInstance, CoInitializeEx, CoUninitialize, CLSCTX_INPROC_SERVER};
use windows::Win32::UI::Accessibility::{CUIAutomation, IUIAutomation, IUIAutomationElement, IUIAutomationTreeWalker};
use windows::Win32::UI::WindowsAndMessaging::{GA_ROOT, GetAncestor, GetClassNameW, PostMessageW, WindowFromPoint};

use crate::overlay::MSG_DESPIERTA;

enum Peticion {
    Cursor(Punto),
    Parar,
}

pub struct Uia {
    envia: SyncSender<Peticion>,
    resultado: Arc<Mutex<Vec<Candidato>>>,
    hilo: Option<JoinHandle<()>>,
}

impl Uia {
    pub fn nueva(notificar: HWND) -> Uia {
        // Capacidad 1: si el hilo esta ocupado, la peticion vieja se pisa.
        let (envia, recibe) = sync_channel::<Peticion>(1);
        let resultado = Arc::new(Mutex::new(Vec::new()));
        let resultado_hilo = Arc::clone(&resultado);
        // HWND no es Send en el crate windows: se pasa el valor crudo.
        let notificar_crudo = notificar.0 as isize;
        let hilo = std::thread::Builder::new()
            .name("pixpin-uia".into())
            .spawn(move || trabajar(recibe, resultado_hilo, notificar_crudo))
            .expect("el hilo UIA deberia poder crearse");
        Uia { envia, resultado, hilo: Some(hilo) }
    }

    /// NUNCA bloquea. Si el canal esta lleno se descarta la peticion vieja
    /// intentando una vez mas; si sigue lleno, se descarta la nueva: la
    /// siguiente llegara en el proximo movimiento del raton.
    pub fn pedir(&self, cursor: Punto) {
        match self.envia.try_send(Peticion::Cursor(cursor)) {
            Ok(()) | Err(TrySendError::Full(_)) | Err(TrySendError::Disconnected(_)) => {}
        }
    }

    /// Copia lo ultimo que el hilo haya resuelto. El Mutex se toma solo
    /// para clonar el Vec: microsegundos.
    pub fn candidatos(&self) -> Vec<Candidato> {
        self.resultado.lock().map(|v| v.clone()).unwrap_or_default()
    }

    pub fn detener(mut self) {
        let _ = self.envia.send(Peticion::Parar);
        if let Some(h) = self.hilo.take() {
            let _ = h.join();
        }
    }
}

impl Drop for Uia {
    fn drop(&mut self) {
        // Si detener() no se llamo, al menos pedir la parada sin join: no
        // bloquear un drop es mas importante que la limpieza perfecta.
        let _ = self.envia.try_send(Peticion::Parar);
    }
}

fn trabajar(recibe: Receiver<Peticion>, resultado: Arc<Mutex<Vec<Candidato>>>, notificar: isize) {
    // SAFETY: cada hilo inicializa COM una vez y lo libera al salir.
    unsafe {
        let _ = CoInitializeEx(None, COINIT_MULTITHREADED);
    }
    let automatizacion: Option<IUIAutomation> =
        // SAFETY: CoCreateInstance del CLSID documentado; None si no hay UIA.
        unsafe { CoCreateInstance(&CUIAutomation, None, CLSCTX_INPROC_SERVER).ok() };

    let mut cache_raiz: Option<(isize, Vec<Candidato>)> = None;

    while let Ok(Peticion::Cursor(p)) = recibe.recv() {
        let mut candidatos = Vec::new();
        if let Some(respaldo) = candidato_respaldo(p) {
            candidatos.push(respaldo.1);
            // Arbol UIA, con cache por ventana raiz.
            if let Some(auto) = &automatizacion {
                let raiz = respaldo.0;
                match &cache_raiz {
                    Some((r, arbol)) if *r == raiz => candidatos.extend(arbol.iter().copied()),
                    _ => {
                        let arbol = arbol_uia(auto, p);
                        candidatos.extend(arbol.iter().copied());
                        cache_raiz = Some((raiz, arbol));
                    }
                }
            }
        }
        if let Ok(mut r) = resultado.lock() {
            *r = candidatos;
        }
        if notificar != 0 {
            // SAFETY: PostMessageW tolera ventanas destruidas; el mensaje es
            // el WM_APP privado del overlay.
            unsafe {
                let _ = PostMessageW(
                    Some(HWND(notificar as *mut _)),
                    MSG_DESPIERTA,
                    Default::default(),
                    Default::default(),
                );
            }
        }
    }
    // SAFETY: empareja el CoInitializeEx de arriba.
    unsafe { CoUninitialize() };
}

/// La ventana raiz bajo el punto y su rectangulo DWM. Nunca GetWindowRect:
/// incluye la sombra y el recuadro saldria mas grande por cada lado.
fn candidato_respaldo(p: Punto) -> Option<(isize, Candidato)> {
    // SAFETY: consultas de solo lectura sobre el estado global de ventanas.
    unsafe {
        let bajo = WindowFromPoint(windows::Win32::Foundation::POINT { x: p.x, y: p.y });
        if bajo.is_invalid() {
            return None;
        }
        let raiz = GetAncestor(bajo, GA_ROOT);
        // Excluir los propios overlays o el snap se ajustaria a si mismo.
        let mut clase = [0u16; 32];
        let n = GetClassNameW(raiz, &mut clase);
        if n > 0 && String::from_utf16_lossy(&clase[..n as usize]) == "PixPinOverlay" {
            return None;
        }
        let mut r = RECT::default();
        DwmGetWindowAttribute(
            raiz,
            DWMWA_EXTENDED_FRAME_BOUNDS,
            &mut r as *mut RECT as *mut _,
            size_of::<RECT>() as u32,
        )
        .ok()?;
        Some((
            raiz.0 as isize,
            Candidato {
                rect: Rect {
                    x: r.left,
                    y: r.top,
                    ancho: (r.right - r.left).max(0) as u32,
                    alto: (r.bottom - r.top).max(0) as u32,
                },
                profundidad: 0,
            },
        ))
    }
}

/// El elemento bajo el punto y sus ancestros, como candidatos de
/// profundidad creciente. Cualquier fallo COM corta y devuelve lo reunido.
fn arbol_uia(auto: &IUIAutomation, p: Punto) -> Vec<Candidato> {
    let mut lista = Vec::new();
    // SAFETY: llamadas COM de solo lectura; cualquier error corta con ok().
    unsafe {
        let Ok(elemento) = auto.ElementFromPoint(windows::Win32::Foundation::POINT { x: p.x, y: p.y }) else {
            return lista;
        };
        let Ok(caminante) = auto.ControlViewWalker() else {
            return lista;
        };
        let caminante: IUIAutomationTreeWalker = caminante;
        // Del elemento hacia la raiz, apuntando la profundidad al reves y
        // corrigiendola al final: no se sabe la profundidad hasta llegar.
        let mut cadena: Vec<Rect> = Vec::new();
        let mut actual: Option<IUIAutomationElement> = Some(elemento);
        while let Some(e) = actual {
            if let Ok(r) = e.CurrentBoundingRectangle() {
                cadena.push(Rect {
                    x: r.left,
                    y: r.top,
                    ancho: (r.right - r.left).max(0) as u32,
                    alto: (r.bottom - r.top).max(0) as u32,
                });
            }
            actual = caminante.GetParentElement(&e).ok();
            // Cota dura: un arbol UIA real no pasa de ~30 niveles; sin esto,
            // un GetParentElement ciclico colgaria el hilo para siempre.
            if cadena.len() > 32 {
                break;
            }
        }
        let total = cadena.len() as u16;
        for (i, rect) in cadena.into_iter().enumerate() {
            lista.push(Candidato { rect, profundidad: total - i as u16 });
        }
    }
    lista
}
```

Añadir a `crates/pixpin-shell/src/lib.rs`: `pub mod uia;`

- [ ] **Step 4: Ejecutar y comprobar que pasa**

Run: `cargo test -p pixpin-shell uia -- --test-threads=1` (el de detener)
Run: `cargo test -p pixpin-shell uia -- --test-threads=1 --ignored` (los dos de escritorio)
Expected: PASS los tres. **Las firmas COM de `IUIAutomation` en el crate `windows` tienen fama de moverse entre versiones: ajusta mecánicamente y repórtalo.**

- [ ] **Step 5: Comprobar el workspace y confirmar**

Run: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace -- --test-threads=1`

```bash
git add crates/pixpin-shell
git commit -m "Hilo de UI Automation con cache: contesta cuando puede, no bloquea nunca

pedir() deja la posicion en un canal de capacidad 1 donde la peticion nueva
pisa a la vieja: solo importa la ultima posicion del raton. El hilo contesta
cuando puede, deja los candidatos bajo un Mutex que solo se toma para
clonar, y despierta al bucle modal con PostMessage. El test del contrato
manda 200 peticiones en rafaga y exige microsegundos por peticion.

El respaldo usa DWMWA_EXTENDED_FRAME_BOUNDS y nunca GetWindowRect, que
incluye la sombra invisible y agranda el recuadro varios pixeles por lado —
el fallo clasico que la spec senala en la competencia. Los overlays propios
se excluyen por clase o el snap se ajustaria a si mismo."
```

---
## Task 8: La sesión viva, con tope de FPS por nivel

El modo en vivo de `Espacio`: una sesión WGC continua cuyo último fotograma el overlay dibuja cuando le llega el aviso. **Primer consumidor real del nivel de rendimiento**: `Completo` deja pasar todos los fotogramas; `Ligero` los limita a 30 por segundo (D14/5.2 del diseño de rendimiento).

**Files:**
- Create: `crates/pixpin-capture/src/sesion.rs`
- Modify: `crates/pixpin-capture/src/lib.rs`

**Interfaces:**
- Consumes: `Dispositivo`, `ErrorCaptura`, `handle_de_monitor` (S1-B1); patrón WGC de `instantanea.rs`.
- Produces:
  - `pub struct SesionViva { … }` con `pub fn nueva(dispositivo: &Dispositivo, id_monitor: u32, minimo_entre_frames: Duration, notificar: Option<(HWND crudo: isize, u32 mensaje)>) -> Result<SesionViva, ErrorCaptura>; pub fn ultimo(&self) -> Option<ID3D11Texture2D>; pub fn cerrar(self)`
  - Contrato de FPS: los fotogramas que lleguen antes de `minimo_entre_frames` desde el último aceptado **se descartan en el manejador**, sin copiar ni notificar. `Duration::ZERO` = sin tope. El llamante deriva el valor del nivel: `Completo` → `ZERO`, `Ligero` → `1000/30 ms`.
  - Cada fotograma aceptado: se guarda la textura bajo `Mutex` (pisa la anterior — una sola copia viva, presupuesto D18) y se notifica con `PostMessage` si se pidió.

- [ ] **Step 1: Escribir los tests que fallan**

Crear `crates/pixpin-capture/src/sesion.rs` con sólo las pruebas:

```rust
#[cfg(test)]
mod pruebas {
    use super::*;
    use crate::dispositivo::Dispositivo;
    use crate::monitores::enumerar_monitores;
    use std::time::{Duration, Instant};

    fn preparar() -> (Dispositivo, u32) {
        let d = Dispositivo::nuevo().unwrap();
        let m = enumerar_monitores().unwrap();
        let principal = m.principal().unwrap().id;
        (d, principal)
    }

    #[test]
    #[ignore = "necesita GPU y sesion de escritorio; ejecutar con --ignored"]
    fn la_sesion_entrega_fotogramas_y_ultimo_los_pisa() {
        let (d, id) = preparar();
        let sesion = SesionViva::nueva(&d, id, Duration::ZERO, None).unwrap();
        // Esperar el primer fotograma con cota.
        let limite = Instant::now() + Duration::from_secs(3);
        let mut primero = None;
        while Instant::now() < limite {
            primero = sesion.ultimo();
            if primero.is_some() {
                break;
            }
            std::thread::sleep(Duration::from_millis(10));
        }
        assert!(primero.is_some(), "no llego ningun fotograma en 3 s");
        sesion.cerrar();
    }

    #[test]
    #[ignore = "necesita GPU y sesion de escritorio; ejecutar con --ignored"]
    fn el_tope_de_fps_descarta_fotogramas() {
        // Con tope de 10 fps, en un segundo no pueden aceptarse mas de ~12
        // (margen para el arranque). El caso negativo es la misma medida sin
        // tope, que en un monitor de 60 Hz acepta bastantes mas: si ambos
        // numeros salieran iguales, el tope no estaria funcionando.
        let (d, id) = preparar();

        let sin_tope = SesionViva::nueva(&d, id, Duration::ZERO, None).unwrap();
        std::thread::sleep(Duration::from_secs(1));
        let aceptados_sin_tope = sin_tope.aceptados();
        sin_tope.cerrar();

        let con_tope = SesionViva::nueva(&d, id, Duration::from_millis(100), None).unwrap();
        std::thread::sleep(Duration::from_secs(1));
        let aceptados_con_tope = con_tope.aceptados();
        con_tope.cerrar();

        assert!(
            aceptados_con_tope <= 12,
            "el tope de 10 fps acepto {aceptados_con_tope} fotogramas en 1 s"
        );
        assert!(
            aceptados_sin_tope > aceptados_con_tope,
            "sin tope ({aceptados_sin_tope}) deberia aceptar mas que con tope ({aceptados_con_tope}); \
             si el escritorio esta completamente estatico WGC puede no entregar fotogramas: \
             mueve algo en pantalla y repite"
        );
    }

    #[test]
    #[ignore = "necesita GPU y sesion de escritorio; ejecutar con --ignored"]
    fn cerrar_detiene_la_entrega() {
        let (d, id) = preparar();
        let sesion = SesionViva::nueva(&d, id, Duration::ZERO, None).unwrap();
        std::thread::sleep(Duration::from_millis(300));
        sesion.cerrar();
        // Si cerrar no cerrara la sesion WGC, el proceso mantendria el
        // dispositivo ocupado y las siguientes pruebas se resentirian; el
        // criterio practico es que no entre en panico y termine.
    }
}
```

(El contador `aceptados()` es API pública de la sesión: `pub fn aceptados(&self) -> u64`. Existe para este test y para el HUD de métricas de S6; no es andamio.)

- [ ] **Step 2: Ejecutar y comprobar que falla**

Run: `cargo test -p pixpin-capture sesion -- --test-threads=1 --ignored`
Expected: FAIL — no existe `SesionViva`.

- [ ] **Step 3: Implementar**

Sigue el patrón de `instantanea.rs` (pool free-threaded, `FrameArrived`, `IDirect3DDxgiInterfaceAccess`), con estas diferencias:

```rust
//! Sesion WGC continua para el modo en vivo del overlay.
//!
//! El manejador de FrameArrived corre en un hilo del pool de WinRT. La
//! textura del ultimo fotograma aceptado se guarda bajo un Mutex, PISANDO
//! la anterior: una sola copia viva (presupuesto D18). El overlay se entera
//! por PostMessage(MSG_DESPIERTA) y dibuja cuando quiere.
//!
//! El tope de FPS es el primer consumidor real del nivel de rendimiento:
//! en Ligero, refrescar a 60 Hz una previsualizacion sobre una iGPU
//! compartida roba la memoria y el bus que la captura final necesita.

use std::sync::atomic::{AtomicU64, Ordering};
use std::sync::{Arc, Mutex};
use std::time::{Duration, Instant};
// ... imports del patron de instantanea.rs mas HWND/PostMessageW ...

pub struct SesionViva {
    sesion: GraphicsCaptureSession,
    pool: Direct3D11CaptureFramePool,
    token: i64,
    ultimo: Arc<Mutex<Option<ID3D11Texture2D>>>,
    aceptados: Arc<AtomicU64>,
}

impl SesionViva {
    pub fn nueva(
        dispositivo: &Dispositivo,
        id_monitor: u32,
        minimo_entre_frames: Duration,
        notificar: Option<(isize, u32)>,
    ) -> Result<SesionViva, ErrorCaptura> {
        // ... crear item + pool igual que capturar_monitor ...
        let ultimo = Arc::new(Mutex::new(None));
        let aceptados = Arc::new(AtomicU64::new(0));
        let ultimo_hilo = Arc::clone(&ultimo);
        let aceptados_hilo = Arc::clone(&aceptados);
        let mut ultimo_instante: Option<Instant> = None;

        let manejador = TypedEventHandler::new(move |pool, _| {
            // Tope de FPS: descartar ANTES de tocar la textura. TryGetNextFrame
            // hay que llamarlo igualmente para drenar el pool, o WGC deja de
            // entregar; lo que se ahorra es la copia, el lock y el PostMessage.
            /* ... TryGetNextFrame; si (ahora - ultimo_instante) < minimo, return Ok(()) ... */
            /* ... extraer ID3D11Texture2D como en instantanea.rs ... */
            /* ... *ultimo_hilo.lock() = Some(textura); aceptados_hilo.fetch_add(1, Ordering::Relaxed) ... */
            /* ... if let Some((hwnd, msg)) = notificar { PostMessageW(...) } ... */
            Ok(())
        });
        // ... FrameArrived + CreateCaptureSession + borde/cursor + StartCapture ...
        todo!("esqueleto: el ejecutor lo completa con el patron de instantanea.rs")
    }

    pub fn ultimo(&self) -> Option<ID3D11Texture2D> {
        self.ultimo.lock().ok().and_then(|g| g.clone())
    }

    pub fn aceptados(&self) -> u64 {
        self.aceptados.load(Ordering::Relaxed)
    }

    pub fn cerrar(self) {
        let _ = self.pool.RemoveFrameArrived(self.token);
        let _ = self.sesion.Close();
        let _ = self.pool.Close();
    }
}
```

**El `todo!` de arriba es del boceto del plan, no del código entregable**: la tarea no está terminada mientras quede un `todo!` en el árbol. El cuerpo completo replica `capturar_monitor` línea a línea salvo en lo señalado.

Añadir a `crates/pixpin-capture/src/lib.rs`:

```rust
pub mod sesion;

pub use sesion::SesionViva;
```

- [ ] **Step 4: Ejecutar y comprobar que pasa**

Run: `cargo test -p pixpin-capture sesion -- --test-threads=1 --ignored`
Expected: PASS los tres. El del tope necesita movimiento en pantalla (un vídeo o mover una ventana); su mensaje de fallo lo explica.

- [ ] **Step 5: Comprobar el workspace y confirmar**

Run: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace -- --test-threads=1`

```bash
git add crates/pixpin-capture
git commit -m "Sesion WGC continua con tope de FPS: el nivel Ligero ya hace algo

El manejador de FrameArrived corre en un hilo del pool de WinRT; la textura
aceptada se guarda pisando la anterior (una sola copia viva, D18) y el
overlay se entera por PostMessage. El tope descarta fotogramas ANTES de
copiarlos: en Ligero, refrescar a 60 Hz una previsualizacion sobre una iGPU
compartida roba la memoria y el bus que la captura final necesita.

TryGetNextFrame se llama tambien para los descartados, porque sin drenar el
pool WGC deja de entregar; lo que se ahorra es la copia y el aviso. El test
compara aceptados con y sin tope en el mismo segundo: si salieran iguales,
el tope seria decorativo."
```

---

## Task 9: «Guardar como», con el diálogo del sistema

`IFileSaveDialog` con los filtros PNG/JPG/WebP. Es la única pieza de esta fase que no se puede probar sin manos: un diálogo modal espera a un humano. Se compensa con la comprobación manual de la Task 12.

**Files:**
- Create: `crates/pixpin-shell/src/guardar.rs`
- Modify: `crates/pixpin-shell/src/lib.rs`, `crates/pixpin-shell/Cargo.toml`

**Interfaces:**
- Consumes: nada interno.
- Produces: `pub fn pedir_ruta_guardado(hwnd_padre: HWND, nombre_sugerido: &str) -> Option<PathBuf>` — `None` si el usuario cancela **o si el diálogo falla** (un fallo COM aquí no debe tirar la captura: el llamante conserva la imagen y puede reintentar).

- [ ] **Step 1: Implementar directamente** (sin test automático posible; la razón queda escrita arriba y el plan lo declara en vez de fingir cobertura)

Añadir `"Win32_UI_Shell_Common"` a las features de `windows` en `pixpin-shell` si no está. Crear `crates/pixpin-shell/src/guardar.rs`:

```rust
//! El dialogo "Guardar como" del sistema, con los filtros de imagen.
//!
//! Unica pieza de S1-B2 sin test automatico: un dialogo modal espera a un
//! humano. La comprobacion es manual (Task 12) y esta declarada, no
//! disimulada.

use std::path::PathBuf;

use windows::Win32::Foundation::HWND;
use windows::Win32::System::Com::{CLSCTX_INPROC_SERVER, CoCreateInstance};
use windows::Win32::UI::Shell::Common::COMDLG_FILTERSPEC;
use windows::Win32::UI::Shell::{
    FileSaveDialog, IFileSaveDialog, SIGDN_FILESYSPATH,
};
use windows::core::{HSTRING, PCWSTR, w};

/// Abre el dialogo y devuelve la ruta elegida, o None si se cancela o el
/// dialogo falla: perder el dialogo no puede costar la captura.
pub fn pedir_ruta_guardado(hwnd_padre: HWND, nombre_sugerido: &str) -> Option<PathBuf> {
    // SAFETY: COM ya esta inicializado en el hilo de interfaz (lo hace la
    // bandeja de S1-A); el dialogo es un objeto local que muere al salir.
    unsafe {
        let dialogo: IFileSaveDialog =
            CoCreateInstance(&FileSaveDialog, None, CLSCTX_INPROC_SERVER).ok()?;
        let filtros = [
            COMDLG_FILTERSPEC { pszName: w!("PNG"), pszSpec: w!("*.png") },
            COMDLG_FILTERSPEC { pszName: w!("JPEG"), pszSpec: w!("*.jpg;*.jpeg") },
            COMDLG_FILTERSPEC { pszName: w!("WebP"), pszSpec: w!("*.webp") },
        ];
        dialogo.SetFileTypes(&filtros).ok()?;
        dialogo.SetDefaultExtension(w!("png")).ok()?;
        let nombre = HSTRING::from(nombre_sugerido);
        dialogo.SetFileName(PCWSTR(nombre.as_ptr())).ok()?;
        // Show devuelve Err al cancelar: es el camino normal, no un fallo.
        dialogo.Show(Some(hwnd_padre)).ok()?;
        let item = dialogo.GetResult().ok()?;
        let ruta = item.GetDisplayName(SIGDN_FILESYSPATH).ok()?;
        let texto = ruta.to_string().ok()?;
        // SAFETY: GetDisplayName reserva con CoTaskMemAlloc; se libera aqui,
        // despues de copiar a String, exactamente una vez.
        windows::Win32::System::Com::CoTaskMemFree(Some(ruta.as_ptr() as *const _));
        Some(PathBuf::from(texto))
    }
}
```

Añadir a `crates/pixpin-shell/src/lib.rs`: `pub mod guardar;`

**Atención a la mina de S1-A:** `GetDisplayName` reserva memoria COM. El `CoTaskMemFree` va **después** de copiar a `String` y antes de cualquier `?` — la fuga de `appdata()` de S1-A fue exactamente un `?` entre la reserva y la liberación. Aquí los `ok()?` van todos antes de la reserva o después de la liberación; si reordenas algo, revisa esto primero.

- [ ] **Step 2: Comprobar que compila limpio y confirmar**

Run: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace -- --test-threads=1`

```bash
git add crates/pixpin-shell
git commit -m "Guardar como con IFileSaveDialog y filtros PNG/JPG/WebP

Devuelve None tanto al cancelar como si el dialogo falla: perder el dialogo
no puede costar la captura, que el llamante conserva. Sin test automatico
posible —un dialogo modal espera a un humano— y declarado en vez de
disimulado: la comprobacion es manual en la Task 12.

El CoTaskMemFree de GetDisplayName va despues de copiar y sin ningun ?
entre medias: la fuga COM de S1-A fue exactamente un ? entre la reserva y
la liberacion."
```

---
## Task 10: La orquestación del overlay en el ejecutable

Donde todo se junta: capturar, abrir un overlay por monitor, dibujar, traducir eventos, y ejecutar la acción final. Sustituye al andamio `capturar_escritorio` de S1-B1.

**Files:**
- Create: `apps/pixpin/src/overlay.rs`
- Modify: `apps/pixpin/src/main.rs`, `apps/pixpin/Cargo.toml`
- Modify: `crates/pixpin-store/i18n/es-ES/main.ftl`, `crates/pixpin-store/i18n/en-US/main.ftl`

**Interfaces:**
- Consumes: todo lo anterior.
- Produces (interno del ejecutable):
  - `pub enum ResultadoOverlay { Region(Rect), Cancelado }`
  - `pub enum ModoConfirmacion { ConBarra, DirectoAlPortapapeles }`
  - `pub fn ejecutar_overlay(dispositivo: &Dispositivo, nivel: Nivel, modo: ModoConfirmacion, textos: &Catalogo, formato_color: FormatoColorLupa) -> anyhow::Result<AccionFinal>`
  - `pub enum AccionFinal { Copiar(Instantanea), Guardar(Instantanea), GuardarComo(Instantanea), Nada }` — la instantánea ya recortada a la región elegida; quien guarda/copia es `main.rs`, reutilizando `a_imagen`/`guardar`/`copiar_imagen` de S1-B1.

**La secuencia, que la spec §2.2 declara innegociable:**

```
atajo → instantanea de TODOS los monitores (queda en GPU)
      → una VentanaOverlay + Superficie + bitmap por monitor
      → mostrar (objetivo < 50 ms desde el atajo; se mide con tracing)
      → bucle_modal hasta Confirmar/Cancelar
      → destruir overlays → recortar la instantanea → AccionFinal
```

La instantánea va **antes** que las ventanas: al revés, la captura se incluiría a sí misma.

**Presupuesto de copias (D18, `Ligero` ≤ 3):** instantánea por monitor (1 por monitor, inevitables), el backbuffer del swapchain (del sistema de composición, no cuenta contra el presupuesto), y la copia de codificación al final. El overlay **no** crea ninguna copia adicional: el fondo es la textura capturada envuelta como bitmap (Task 4) y la lupa dibuja **la misma textura** reescalada con `dibujar el bitmap con rect fuente/destino` e interpolación `NEAREST_NEIGHBOR` — píxeles reales, como exige la spec.

**Textos nuevos** (`Step 1`): `barra-copiar`, `barra-guardar`, `barra-guardar-como`, `barra-descartar` en ambos catálogos (`Copiar/Copy`, `Guardar/Save`, `Guardar como…/Save as…`, `Descartar/Discard`). El test de paridad de S1-A vigila que no falte ninguno.

**Qué se dibuja en cada `Pintar`** (todo con el `MotorRender`, por monitor):

1. El bitmap de fondo: congelado = instantánea; vivo = `sesion.ultimo()` si hay, si no la instantánea.
2. El velo `Color::oscurecido()` sobre todo el monitor **excepto** la selección (4 rectángulos alrededor: más simple y más rápido que capas o geometrías).
3. El borde de la selección (`Color::ACENTO`, 2 px lógicos × escala) y los 8 tiradores (cuadrados de 8 px lógicos, blancos con borde acento) — sólo en `Lista`/gestos; en `Explorando`, el `rect_resaltado()` con borde discontinuo (`ID2D1StrokeStyle` con guiones).
4. El texto de dimensiones y coordenadas `{ancho}×{alto} ({x}, {y})` junto a la esquina superior de la selección (DirectWrite, fondo semitransparente) — la spec pide dimensiones **y coordenadas** en vivo.
5. La lupa (si el cursor está en este monitor): bitmap fuente = región `lupa.region_fuente()` de la textura, destino = cuadrado en `lupa.colocar()`, `NEAREST_NEIGHBOR`; retícula de cruz en el centro; debajo, `texto_color(...)` del píxel central — el color se lee de un mapa CPU de **la región de la lupa** (22×22 px, no la pantalla entera: `Instantanea::recortar` + `a_imagen` sólo de esa miniatura, una vez por movimiento de ratón como mucho).
6. La barra de resultado (sólo en `Lista` y `ModoConfirmacion::ConBarra`): fondo redondeado, botones con texto Fluent, resaltado del botón bajo el cursor.

**Sin animaciones — en ningún nivel.** El diseño de rendimiento (5.2) las lista como degradables; en S1-B2 se decide no implementarlas en absoluto, y este párrafo existe para que nadie las añada «de gratis» en la revisión.

- [ ] **Step 1: Textos y dependencias**

Añadir las cuatro claves a ambos catálogos y el test correspondiente en `idioma.rs` (mismo patrón que `los_textos_de_captura_estan_en_los_dos_idiomas`, con las claves `barra-*`). Añadir a `apps/pixpin`:

```bash
cargo add pixpin-render --path crates/pixpin-render -p pixpin
cargo add pixpin-ui --path crates/pixpin-ui -p pixpin
cargo add pixpin-geom --path crates/pixpin-geom -p pixpin
```

Run: `cargo test -p pixpin --test capas -- --test-threads=1` — Expected: PASS (L4 depende de todo).
Run: `cargo test -p pixpin-store idioma -- --test-threads=1` — Expected: FAIL hasta añadir las claves; PASS después.

- [ ] **Step 2: Escribir `overlay.rs` del ejecutable**

No hay test unitario del módulo entero —es cableado sobre piezas ya probadas— pero sí **una invariante extraída a función pura con test**, porque es donde estará el error: qué monitor dibuja qué parte de la selección y de la lupa.

En `apps/pixpin/src/overlay.rs`, la función y su test:

```rust
/// Que parte de un rectangulo global le toca dibujar a este monitor, en
/// coordenadas locales del monitor.
///
/// La seleccion puede cruzar monitores; cada overlay recorta y traduce.
fn parte_local(global: Rect, monitor: Rect) -> Option<Rect> {
    global.interseccion(monitor).map(|r| Rect {
        x: r.x - monitor.x,
        y: r.y - monitor.y,
        ancho: r.ancho,
        alto: r.alto,
    })
}

#[cfg(test)]
mod pruebas {
    use super::*;
    use pixpin_geom::Rect;

    #[test]
    fn una_seleccion_que_cruza_dos_monitores_se_reparte_bien() {
        let izquierdo = Rect { x: -1920, y: 0, ancho: 1920, alto: 1080 };
        let derecho = Rect { x: 0, y: 0, ancho: 1920, alto: 1080 };
        let seleccion = Rect { x: -100, y: 100, ancho: 300, alto: 200 };

        let en_izq = parte_local(seleccion, izquierdo).unwrap();
        let en_der = parte_local(seleccion, derecho).unwrap();
        // En el monitor izquierdo (que empieza en -1920), x local = 1820.
        assert_eq!(en_izq, Rect { x: 1820, y: 100, ancho: 100, alto: 200 });
        assert_eq!(en_der, Rect { x: 0, y: 100, ancho: 200, alto: 200 });
        // Caso negativo: un monitor que no toca la seleccion no dibuja nada.
        let tercero = Rect { x: 0, y: -1080, ancho: 1920, alto: 1080 };
        assert_eq!(parte_local(seleccion, tercero), None);
    }
}
```

El resto del módulo, en este orden y con estas decisiones cerradas:

```rust
//! La orquestacion del overlay: capturar, mostrar, interactuar, decidir.
//!
//! Secuencia innegociable (spec 2.2): la instantanea de TODOS los monitores
//! se toma ANTES de crear ventana alguna; al reves, la captura se incluiria
//! a si misma. El objetivo atajo->overlay visible es < 50 ms y se deja
//! medido en el log con dos marcas de tracing.

pub struct PiezasMonitor {
    monitor: pixpin_geom::Monitor,
    instantanea: pixpin_capture::Instantanea,
    ventana: pixpin_shell::overlay::VentanaOverlay,
    superficie: pixpin_render::Superficie,
    fondo: ID2D1Bitmap1,           // instantanea envuelta, sin copia
    fondo_vivo: Option<ID2D1Bitmap1>, // el ultimo frame vivo, si Espacio esta activo
}

pub fn ejecutar_overlay(/* firma de arriba */) -> anyhow::Result<AccionFinal> {
    let t0 = std::time::Instant::now();
    let disposicion = pixpin_capture::enumerar_monitores()?;

    // 1. Instantanea de todos los monitores, en GPU.
    let mut piezas: Vec<PiezasMonitor> = /* capturar_monitor por cada monitor,
        crear VentanaOverlay + MotorRender (uno global) + Superficie + fondo */;

    // 2. Estado puro + UIA + (aun) sin sesion viva.
    let mut estado = pixpin_ui::EstadoOverlay::nuevo(disposicion.clone());
    let uia = pixpin_shell::uia::Uia::nueva(piezas[0].ventana.handle());
    let mut sesiones_vivas: Vec<Option<pixpin_capture::SesionViva>> = /* None por monitor */;

    // 3. Mostrar y medir.
    for p in &piezas { p.ventana.mostrar(); }
    tracing::info!(ms = t0.elapsed().as_millis() as u64, "overlay visible");

    // 4. Bucle modal: traducir EventoOverlay -> EventoEntrada, procesar,
    //    actuar segun Efecto, redibujar solo lo invalidado.
    //    - RatonMovido: ademas uia.pedir(p) y, si el cursor cambio de
    //      monitor, invalidar el monitor que pierde la lupa y el que la gana.
    //    - Despierta: estado.procesar(Candidatos(uia.candidatos())) y, en
    //      vivo, refrescar fondo_vivo con sesion.ultimo() del monitor activo.
    //    - Tecla: traducir vk (VK_LEFT.., VK_RETURN, VK_ESCAPE, VK_SPACE)
    //      con el x1/x10 de shift AQUI, no en pixpin-ui.
    //    - Efecto::AlternarVivo: crear las SesionViva (tope segun nivel:
    //      Completo ZERO, Ligero 33 ms) o cerrarlas todas al volver a congelar.
    //    - Efecto::Confirmar en modo ConBarra: pasar a un sub-estado
    //      "barra visible" en el que los clics van primero a barra.boton_en;
    //      en DirectoAlPortapapeles: salir ya con AccionFinal::Copiar.
    //    - Pintar / invalidaciones: redibujar SOLO los monitores tocados.
    //      (Invalidacion por region monitor a monitor: el presupuesto no
    //      permite redibujar tres 4K por mover un raton un pixel.)

    // 5. Salida: cerrar sesiones vivas, detener uia, drop de piezas
    //    (destruye ventanas), recortar la instantanea del monitor (o
    //    monitores) elegidos y devolver AccionFinal.
    //    Si la region cruza monitores, se recorta de cada instantanea y se
    //    compone en una textura destino con CopySubresourceRegion — en GPU,
    //    como todo lo demas.
}
```

Este pseudocódigo del bucle es el **contrato**; el cuerpo es tuyo. Las piezas ya están todas probadas: la tarea es cablearlas sin inventar comportamiento nuevo. Si en el cableado descubres que falta una primitiva (un método de dibujo, un evento), **añádela en su crate con su test**, no aquí con un `unsafe` de contrabando: `apps/pixpin` sigue siendo `forbid(unsafe_code)`.

- [ ] **Step 3: Cablear `main.rs`**

Sustituir el cuerpo del brazo `Evento::Atajo(id) if id == atajos::ID_REGION` para llamar a `ejecutar_overlay(..., ModoConfirmacion::ConBarra, ...)` y ejecutar la `AccionFinal` (guardar reutiliza el contador `captura-NNNN.png` y `carpeta_capturas` de los ajustes; copiar reutiliza `copiar_imagen`; `GuardarComo` pasa por `pedir_ruta_guardado` y `FormatoImagen::por_extension`, cayendo a PNG si la extensión no se reconoce). `capturar_escritorio` (el andamio de S1-B1) **se borra**: su comentario ya anunciaba que esta fase lo sustituía.

- [ ] **Step 4: Ejecutar los tests y compilar**

Run: `cargo test --workspace -- --test-threads=1`
Expected: PASS (incluido `parte_local` y los textos nuevos).

Run: `cargo build --release`
Expected: compila sin avisos.

- [ ] **Step 5: Comprobación manual — la primera vez que el overlay existe**

Ejecuta `target\release\pixpinmax.exe`, pulsa `Ctrl+Alt+X` y comprueba **en este orden** (para cada punto, si falla, anota exactamente qué se ve):

1. La pantalla se congela con velo oscuro; la zona bajo el cursor resalta ventanas/controles con borde discontinuo al mover el ratón.
2. Arrastrar traza una selección nítida (sin velo dentro); los 8 tiradores aparecen al soltar; el texto `{ancho}×{alto}` acompaña.
3. La lupa sigue al cursor con píxeles **reales** (cuadraditos nítidos, no borrosos) y el color HEX correcto — comprueba sobre un color conocido (el rojo de cerrar de una ventana).
4. Clic sin arrastre sobre una ventana la selecciona entera; el recuadro NO incluye la sombra (borde pegado al marco visible).
5. Flechas mueven 1 px; con `Shift`, 10. `Espacio` pasa a vivo (reproduce un vídeo y compruébalo); otro `Espacio` congela.
6. `Enter` o los botones de la barra: **Copiar** deja la imagen pegable en Paint; **Guardar** crea `captura-NNNN.png`; **Guardar como…** abre el diálogo con los tres filtros y guarda donde digas; **Descartar** cierra sin rastro. `Esc` cancela en cualquier momento.
7. Con el monitor secundario conectado (si lo hay): el overlay cubre ambos, la selección cruza de uno a otro, y la lupa no se ve borrosa en ninguno.
8. En el log: la línea `overlay visible` con sus ms.

- [ ] **Step 6: Confirmar**

Run: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace -- --test-threads=1 && cargo deny check licenses bans sources`

```bash
git add -A
git commit -m "El overlay de seleccion existe: congelar, elegir, confirmar

Secuencia de la spec: instantanea de todos los monitores ANTES de crear
ventana alguna (al reves la captura se incluiria a si misma), una ventana
por monitor con su superficie de composicion, y bucle modal sobre el estado
puro de pixpin-ui. El fondo es la textura capturada envuelta como bitmap:
cero copias nuevas. La lupa dibuja esa misma textura con NEAREST_NEIGHBOR:
pixeles reales, y el color se lee mapeando solo la miniatura de 22x22.

La invalidacion va monitor a monitor: mover el raton no redibuja tres 4K.
El andamio capturar_escritorio de S1-B1 muere aqui, como anunciaba su
comentario."
```

---

## Task 11: `Ctrl+Alt+C` — capturar y copiar sin ceremonia

El segundo atajo del catálogo: mismo overlay, pero al confirmar va directo al portapapeles y desaparece, sin barra.

**Files:**
- Modify: `apps/pixpin/src/main.rs`

- [ ] **Step 1: Cablear**

En el bucle de eventos, el brazo `id == atajos::ID_COPIAR` llama a `ejecutar_overlay(..., ModoConfirmacion::DirectoAlPortapapeles, ...)` y ejecuta sólo `AccionFinal::Copiar`. La distinción ya existe desde la Task 10; esto es un brazo de `match`.

- [ ] **Step 2: Comprobar a mano**

`Ctrl+Alt+C`, seleccionar, `Enter` → el overlay desaparece sin barra y Paint pega la región. `Esc` → no toca el portapapeles (compruébalo copiando un texto antes: debe seguir ahí).

- [ ] **Step 3: Confirmar**

Run: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace -- --test-threads=1`

```bash
git add apps/pixpin
git commit -m "Ctrl+Alt+C: el mismo overlay, directo al portapapeles

La distincion ConBarra/DirectoAlPortapapeles ya existia en la orquestacion;
este cableado es un brazo de match. Esc no toca el portapapeles: descartar
no puede costar lo que hubiera copiado antes el usuario."
```

---

## Task 12: Puertas de la fase y medición

Las tres puertas que el diseño de rendimiento exige a esta fase, más el informe.

**Files:**
- Create: `apps/pixpin/tests/overlay_no_muta.rs`
- Create: `medidas/AAAA-MM-DD-<maquina>.md` (con la fecha real)
- Modify: el plan (casillas) y, si algún número provisional se confirma o se desmiente, la spec de rendimiento.

- [ ] **Step 1: La puerta de los bytes — mostrar no muta**

Crear `apps/pixpin/tests/overlay_no_muta.rs`:

```rust
//! El tercer intocable de D14: mostrar la captura no la muta.
//!
//! Guardar tras pasar por el overlay debe producir EXACTAMENTE los mismos
//! bytes que guardar sin overlay. Si el render escribiera en la textura
//! capturada (un SetTarget equivocado bastaria), el PNG cambiaria y este
//! test lo caza comparando byte a byte.

use pixpin_capture::{Dispositivo, a_imagen, capturar_monitor, enumerar_monitores};
use pixpin_geom::Rect;
use pixpin_render::MotorRender;

#[test]
#[ignore = "necesita GPU y sesion de escritorio; ejecutar con --ignored"]
fn dibujar_la_instantanea_no_cambia_sus_bytes() {
    let d = Dispositivo::nuevo().unwrap();
    let m = enumerar_monitores().unwrap();
    let principal = *m.principal().unwrap();
    let inst = capturar_monitor(&d, principal.id, principal.area).unwrap();
    let region = Rect { x: principal.area.x + 40, y: principal.area.y + 40, ancho: 200, alto: 150 };

    // Antes de dibujar nada.
    let antes = a_imagen(&d, &inst.recortar(&d, region).unwrap()).unwrap();

    // Envolver la textura como fondo y dibujarla entera sobre un destino
    // aparte, como hace el overlay en cada Pintar.
    let motor = MotorRender::nuevo(d.d3d()).unwrap();
    let fondo = motor.bitmap_desde_textura(inst.textura()).unwrap();
    let destino_tex = /* crear una textura RENDER_TARGET del mismo tamano,
        con el mismo helper que uses en pixpin-render para los tests */;
    let destino = motor.destino_desde_textura(&destino_tex).unwrap();
    let c = motor.contexto();
    // SAFETY: protocolo SetTarget/BeginDraw/EndDraw sobre objetos vivos.
    unsafe {
        c.SetTarget(&destino);
        c.BeginDraw();
        c.DrawBitmap(&fondo, None, 1.0,
            windows::Win32::Graphics::Direct2D::D2D1_INTERPOLATION_MODE_NEAREST_NEIGHBOR,
            None, None);
        c.EndDraw(None, None).unwrap();
        c.SetTarget(None);
    }

    // Despues de dibujar: los mismos bytes, o el overlay esta mutando.
    let despues = a_imagen(&d, &inst.recortar(&d, region).unwrap()).unwrap();
    assert_eq!(antes.pixeles, despues.pixeles, "el render muto la textura capturada");
}
```

(El `/* crear una textura ... */` se resuelve exportando desde `pixpin-render` un helper `pub fn textura_destino(d3d, ancho, alto)` **con su propia doc**, o duplicando las 15 líneas del descriptor en el test: el ejecutor elige y lo deja escrito. `apps/pixpin` es `forbid(unsafe_code)`, pero **sus tests de integración** son binarios aparte donde el `forbid` del `main.rs` no aplica; aun así, si el test necesita `unsafe`, cada bloque lleva su `// SAFETY:` como en cualquier crate auditado.)

Run: `cargo test -p pixpin --test overlay_no_muta -- --test-threads=1 --ignored`
Expected: PASS.

- [ ] **Step 2: Medir y escribir el informe**

Con el binario release, anotar en `medidas/AAAA-MM-DD-<maquina>.md` (mismo formato que el informe de S1-B1):

| Métrica | Objetivo | Cómo se mide |
|---|---|---|
| Atajo → overlay visible | **< 50 ms** (sagrado) | la línea `overlay visible` del log, 5 muestras, se anota la peor |
| CPU en reposo con overlay abierto y congelado | 0 % | Administrador de tareas, 10 s quieto |
| RAM con overlay abierto (1 monitor 3000×2000) | anotar (sin objetivo aún: primera medición) | `Get-Process` |
| Fotogramas aceptados en vivo, `Completo` vs forzado `Ligero` | Ligero ≈ 30/s, Completo > Ligero | contador `aceptados()` expuesto en el log al cerrar la sesión |
| Selección arrastrada a 60 Hz | sin tirones perceptibles | a ojo, y anotado como subjetivo |

**Anota los valores reales aunque no cumplan.** Si el atajo→overlay no baja de 50 ms, la fase NO se cierra: es un intocable, y el plan de ataque (precalentar más, aplazar la creación de superficies, medir qué paso pesa) se decide con los números delante.

- [ ] **Step 3: Cerrar la fase**

Run: `cargo fmt --all --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace -- --test-threads=1 && cargo test --workspace -- --test-threads=1 --ignored && cargo deny check licenses bans sources`

```bash
git add apps/pixpin/tests medidas
git commit -m "Puerta de los bytes del overlay y medicion de la fase

Mostrar no muta: guardar tras pasar por el overlay produce exactamente los
mismos bytes que guardar sin overlay, comprobado byte a byte con GPU real.
Las medidas de la fase quedan en medidas/, incumplimientos incluidos si los
hay: un intocable que no se cumple no se maquilla, se ataca con los numeros
delante."
```

---

## Definición de terminado para S1-B2

- [ ] `cargo fmt --all --check` en verde
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` en verde
- [ ] `cargo test --workspace -- --test-threads=1` en verde
- [ ] `cargo test --workspace -- --test-threads=1 --ignored` en verde en una máquina con escritorio y GPU
- [ ] `cargo deny check licenses bans sources` en verde
- [ ] El test de capas sigue pasando con las dependencias nuevas; `baseline.rs` sigue en verde
- [ ] Los catálogos de idioma siguen sincronizados, con los textos de la barra
- [ ] `Ctrl+Alt+X`: congelar → seleccionar (arrastre, clic-a-ventana, teclado, tiradores) → barra con las cuatro acciones funcionando
- [ ] `Ctrl+Alt+C`: directo al portapapeles, y `Esc` no toca el portapapeles
- [ ] La lupa enseña píxeles reales (nítidos) y el color correcto en el formato configurado
- [ ] `Espacio` alterna congelado/vivo; en `Ligero` forzado, el contador de fotogramas confirma el tope de 30
- [ ] El snap resalta ventanas y controles, sin incluir la sombra de las ventanas
- [ ] Cerrar un overlay no mata la aplicación (test) y `Esc` devuelve la app a la bandeja intacta
- [ ] La puerta de los bytes (`overlay_no_muta`) en verde
- [ ] `atajo → overlay visible < 50 ms` medido y anotado en `medidas/` — **si no se cumple, la fase no se cierra**
- [ ] Las métricas del Step 2 de la Task 12 anotadas con valores reales y commiteadas

**Lo siguiente:** plan **S1-B3** — captura con scroll (`Ctrl+Alt+S`: correlación y cosido en L0 con fixtures de PNGs versionados, cabeceras fijas, parada automática) y cuentagotas global (`Ctrl+Alt+D`, reutilizando la lupa). Después, S1-C: ajustes con interfaz, carpeta configurable y plantillas de nombre.
